# 错误处理 — 行为基线

> **最后更新**：—
> **最后变更**：—

## 模块概述

- **职责**：跨模块错误传播、恢复策略、断电恢复、降级运行
- **对外接口**：错误回调注册、错误日志接口
- **约束**：错误不得静默吞掉、关键路径必须有 fallback

## 核心数据结构

<!-- TODO: 填充错误码体系、错误日志结构、Fault 注入接口等 -->

## 行为描述

### 错误传播链路

<!-- TODO: 填充 NAND → FTL → NVMe 的错误传播路径、错误码映射 -->

### NAND ECC 错误恢复

<!-- TODO: 填充可纠正/不可纠正的恢复策略、重试次数、弱块标记阈值 -->

### DMA/中断错误恢复

<!-- TODO: 填充 DMA 超时重试、中断丢失时的轮询 fallback -->

### 命令级错误处理

<!-- TODO: 填充 NVMe 命令超时、设备内部错误的状态码返回 -->

### 断电恢复

<!-- TODO: 填充映射表恢复流程、GC 中断恢复、数据一致性校验 -->

### 降级运行

<!-- TODO: 填充坏块超阈值、ECC 能力不足时的降级策略 -->

## 接口契约

<!-- TODO: 填充错误回调注册接口、错误码定义 -->

## 依赖关系

- 依赖模块：所有模块（错误产生方）、Platform（中断超时检测）
- 被依赖模块：所有模块（错误消费方）

## 已知限制

<!-- TODO: 填充恢复时间上限、可恢复错误最大重试次数、断电恢复窗口等 -->
