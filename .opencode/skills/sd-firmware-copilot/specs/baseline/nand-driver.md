# NAND 驱动层 — 行为基线

> **最后更新**：—
> **最后变更**：—

## 模块概述

- **职责**：NAND Flash 底层操作——Page 级读写、Block 级擦除、ECC、坏块管理
- **对外接口**：`nand_read_page()`、`nand_write_page()`、`nand_erase_block()`
- **约束**：同一 Page 不可写入两次（NAND 物理限制）、操作必须遵守时序

## 核心数据结构

<!-- TODO: 填充 NandCtx、操作队列、命令描述符、DMA 缓冲管理等 -->

## 行为描述

### Page 读取流程

<!-- TODO: 填充读命令发送、数据 DMA 传输、ECC 校验、重试逻辑 -->

### Page 写入流程

<!-- TODO: 填充写前检查（Block 状态）、数据编码、写入提交、状态确认 -->

### Block 擦除流程

<!-- TODO: 填充擦除前数据迁移检查、命令发送、擦除确认、擦除计数更新 -->

### ECC 与错误处理

<!-- TODO: 填充 ECC 可纠正/不可纠正路径、弱块标记、坏块重映射 -->

### 坏块管理

<!-- TODO: 填充坏块初始化扫描、运行时坏块检测、坏块表更新、Spare 块选择 -->

## 接口契约

<!-- TODO: 填充公开函数签名、前置/后置条件、DMA Buffer 规范 -->

## 依赖关系

- 依赖模块：Platform（寄存器、中断、DMA、时钟）
- 被依赖模块：FTL 层

## 已知限制

<!-- TODO: 填充 NAND 特定限制（Plane/CE 并发、时序约束、ECC 能力上限、坏块率上限） -->
