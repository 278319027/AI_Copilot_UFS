# Coding Style Rules

## 1. 命名规范

- 类型使用清晰、稳定的前缀。
- 函数名体现模块与动作。
- 宏名全部大写并具有明确语义。

## 2. 文件规范

- 每个 `.c` 文件对应一个清晰职责。
- `.h` 文件只暴露必要接口。
- 头文件必须可独立包含。

## 3. 接口规范

- API 必须有明确的输入、输出和错误码。
- 参数必须校验。
- 返回值必须定义清楚。

