# AI 辅助 UFS/SSD 固件开发平台 · 总体设计

**版本：V2.0**
**状态：Design Baseline（取代 V1.0 的总体设计地位）**
**定位：企业内部 AI 辅助 UFS/SSD Firmware 开发平台的总体架构与契约基准**
**核心栈：Comet / OpenCode / CodeGraph / graphify / OpenViking / Plugin & Hook / 本地 LLM**

---

## 0. 文档说明

### 0.1 本册的地位

本册是**总体设计基准**。它定义平台的设计目标、总体架构、生命周期对齐、Agent 与 Skill 的名册与边界、产物权威来源、状态机与权限模型。

**后续所有 Agent / Skill / Hook / Plugin / 产物设计都必须从本册向下分解，不再独立设计。**

分册只做**展开**，不得与本册冲突。若分册发现本册有误，先改本册，再改分册。

### 0.2 与 V1.0 的关系

| 对象 | 处置 |
|---|---|
| `docs/AI辅助UFS_SSD固件开发_ATDD_Comet_完整设计文档_V1.0.md` | **原样保留**，降级为历史版本，不再作为权威 |
| 本册 V2.0 | 取代 V1.0 的权威地位 |
| V1.0 的 Agent 名册（第 38-39 节） | **作废**，见本册 §6 |
| V1.0 的"五阶段"表述 | **修正**为 phase=5 / node=8 双轨，见本册 §2.7 |

V1.0 中仍然有效并已被本册吸收的内容，在附录 B 的来源映射表中逐条标注。

### 0.3 分册索引

| 分册 | 内容 | 回答的问题 |
|---|---|---|
| **00（本册）** | 总体设计 | 平台是什么、怎么分层、谁在什么时候做什么 |
| [`01-方法论-ATDD与测试设计.md`](01-方法论-ATDD与测试设计.md) | 方法论层 | 行为怎么定义、怎么验证、怎么实现 |
| [`02-Skill规范.md`](02-Skill规范.md) | Skill 规范 | 每个 Skill 的输入输出与硬规则是什么 |
| [`03-Comet实现设计-节点特化·Plugin·Hook.md`](03-Comet实现设计-节点特化·Plugin·Hook.md) | 平台实现层 | Comet 节点如何特化、能力与强制如何落地 |
| [`04-Stage0-需求分解与Challenge.md`](04-Stage0-需求分解与Challenge.md) | Stage 0 | 需求如何被拆成可验收基线并冻结 |

### 0.4 参考资料与引用方式

四份 `.txt` 讨论稿与 `V1.0` 文档是**方法论的参考资料**，不是设计本身。本设计文档集对它们做了三件事：

> **融合分类 → 对不一致之处裁定共识 → 输出设计。**

引用方式约定如下：

- **正文不逐节标注来源。** 设计陈述直接给出结论。读者不需要在每一节看到"这条出自哪份文档的哪一行"——那是审计报告的写法，不是设计文档的写法。
- **参考资料在文档级列出**，见 [附录 B](#附录-b参考资料)。
- **参考资料之间的分歧与裁定**集中记录于 [附录 A](#附录-a关键设计裁定)，含各方说法与裁定理由。
- 凡参考资料未给出、或相互冲突之处，本设计**直接给出裁定**。**本设计文档集不设"待评审 / 待裁决"状态。**
- 只有**依赖本设计之外的事实**的事项（例如本仓库的实际布局、Comet 的具体版本、某个 CLI 是否已安装）列为"外部依赖"，见 [附录 C](#附录-c外部依赖事项)。
- 引用 Comet 本体时直接写文件名（如 `classic-state.ts`、`builtins.ts`），完整路径见附录 B。

---

## 1. 设计目标

### 1.1 问题陈述

AI 辅助开发的典型链路是：
```text
需求 → Agent 理解 → Agent 设计 → Agent 写代码 → Agent 自测
```

本平台要解决的问题**不是"写得慢"**，而是这条链路上的五种结构性失效：
| # | 失效模式 | 后果 |
|---|---|---|
| **P1** | 每一跳都可能产生新的"解释" | 最终实现偏离原始需求，且无人察觉 |
| **P2** | Agent 自己定义需求、自己定义"通过"的标准 | 验收标准被实现方稀释 |
| **P3** | 测试沦为获取 GREEN 的手段 | `Test Failed → 修改 Expected → PASS` |
| **P4** | 代码/测试与需求之间无可机器检查的追溯链 | 无法回答"这行代码为什么存在" |
| **P5** | 照搬通用软件 TDD 到固件 | 硬件绑定 / 中断 / DMA / 时序逻辑无法纯 UT，导致测试失真或造假 |


### 1.2 平台定义

> **以 ATDD 为需求契约，以 Comet 为生命周期，以 Agent 为执行主体，以 Skill 为工程方法，以 CodeGraph 为代码事实，以 OpenViking 为企业知识，以 TDD 为实现纪律，以 Verify/Evidence 为最终闭环的 AI Firmware Development Platform。**

端到端流程：
```text
Requirement → Acceptance Contract → Architecture/Design → Verification Design
 → Implementation Plan → TDD Implementation → Verification → Evidence → Archive
```

**命名空间约定（缺陷码见 `98` 的 G06 / G07；本节为全文档集强制约定）**：

| 名称 | 指什么 | 写法 |
|---|---|---|
| **OpenCode** | **宿主**（Agent / Skill / Plugin / Hook 的运行平台与权限执行者） | 不写 `OpenCode` 的其它拼写；**不是**目录 |
| **OMO Team Mode** | **协作运行时**（多 Agent 并行协作机制），由本设计在 `design` 节点按条件启用（`99 D45`） | 首次出现写全称，后文可写 `OMO` |
| **`.opencode/`** | **目录**（`agents/`、`skills/`、`plugins/`、`hooks/`、`config`） | 一律带前导点与斜杠；不用于指代宿主或运行时 |
| **Agent** | 执行主体，名册固定 13 个，一律 `ufs-*`（2 个 Stage 0 用 `ufs-requirements` / `ufs-challenge`） | 引用时写 **`Agent \`ufs-x\``** 或 **`④ ufs-test-design`**（编号 + 名） |
| **Skill** | 工程方法，名字与部分 Agent **同名**（`ufs-challenge` / `ufs-code-review` / `ufs-test-design` / `ufs-verification`） | 引用时写 **`Skill \`ufs-x\``**；仅写反引号名不足以区分时，必须带 `Agent` / `Skill` 限定词 |
| **节点 vs 阶段** | `design` / `plan` 等是 **node**；`design` / `build` 等是 **phase** | "跳过 `design` 节点"指 **node**；"`tweak` 跳过 design"指 **phase**——必要时写成"跳过 `design` 阶段（因而不执行 `design` 节点）" |

> **preset 计数**：Comet 定义 **4 个 preset**（`full` / `tweak` / `hotfix` / `any`），其中 `any` 是 Skill 创建向导、不承载本方法论（§2.4）。因此本设计常说"**三种 preset**"——特指 `full` / `tweak` / `hotfix`。两处表述都合法，但**不得**说"三种 preset"而把 `any` 计入或漏掉。


### 1.3 设计目标（G1–G5）

| # | 目标 | 判定方式 | 性质 |
|---|---|---|---|
| **G1** | 需求不可被 Agent 重新解释 | 需求进入工程前冻结为 AC；任一阶段静默改写 AC 即为违规 | |
| **G2** | 每份产出都可追溯到需求 | `REQ→Capability→Story→AC→Scenario→Design→Implementation→Test→Evidence` 全链机器可检查；断链进入审查而非默认合理 | |
| **G3** | 证据先于完成 | 铁律 `No new behavior without executable evidence`；"测试通过"不等于完成，归档需多重条件同时成立 | |
| **G4** | 方法论与平台解耦 | Comet 管 WHEN、Agent 管 WHO、Skill 管 HOW、Plugin 管能力、Hook 管强制、Human 管决策，互不越权 | |
| **G5** | 适配固件现实 | 先判 Verification Level 再决定测试形态；Legacy 代码先建 Characterization Baseline 再进 TDD | |

### 1.4 非目标（`NT-1`…`NT-9`；`NT` = Non-Target）

> **命名说明**：本节条目编号用 **`NT-`**，与 §5.1.1 的五张子表 **`表 N1`…`表 N5`** 分属不同符号空间（后者是命名空间表，不是条目编号）。

| # | 不做 | 依据 |
|---|---|---|
| **NT-1** | 不做"自动写更多代码"的通用 AI Coding Agent | V1.0 §1 |
| **NT-2** | 不做通用软件平台 | 范围锁 UFS/SSD Firmware、Embedded C、ARM Controller、Simulator |
| **NT-3** | 不让 Agent 决定产品目标与关键架构 | `Human = DECISION` |
| **NT-4** | 不做十余个平级自主 Agent | 受控模型：仅 Main 持有生命周期，其余为窄职责 subagent |
| **NT-5** | 不让 Comet 自己实现软件工程方法论 | 正确形态是 `Comet → Node → Skill` |
| **NT-6** | 不把 Skill 当知识库 | 知识归 OpenViking |
| **NT-7** | 不让 Plugin 绕过 Hook 的治理 | 架构稿 L2920-L2967 |
| **NT-8** | 不重建 Agent Mailbox / 协作协议 | 复用 OMO |
| **NT-9** | 不用测试覆盖率作为完成的唯一判据 | 方法论稿：Coverage 可作指标，不能作唯一完成条件 |

### 1.5 成功判据（S1–S5）

- **S1** 任一代码/测试变更可定位到 AC；任一 AC 可定位到 Evidence
- **S2** 断链可被 Hook 机器检出，且 fail-closed
- **S3** 归档条件全部满足才允许归档（见 §4.8）
- **S4** 失败按 `TEST/CODE/DESIGN/ENVIRONMENT` 四类回流，不在错误阶段偷偷修复
- **S5** 阶段推进由 guard 强制，不依赖 Agent 自觉

---

## 2. Comet 基线（本设计的对齐对象）

本设计的生命周期**不自创**，一律对齐 Comet 本体。本章是后续所有章节的坐标系。

### 2.1 五阶段是真常量

```typescript
// domains/comet-classic/classic-state.ts
const PHASES = ['open', 'design', 'build', 'verify', 'archive'] as const;
```


### 2.2 迁移表

| 事件 | from | guard | to |
|---|---|---|---|
| `open-complete` | open | `open-artifacts-present` | **design（full）／build（hotfix·tweak）** |
| `design-complete` | design | `design-evidence-present` | build |
| `build-complete` | build | `build-decisions-selected` | verify |
| `verify-pass` | verify | `verification-report-present` | archive |
| **`verify-fail`** | verify | `verification-failed` | **build（原生回流）** |
| **`preset-escalate`** | build | `preset-workflow` | **升到 full** |
| `archive-confirm` | archive | `archive-final-confirmation` | archive（**不换阶段**） |
| `archive-reopen` | archive | `archive-not-finalized` | **verify** |
| `archived` | archive | `verify-result-pass` + `archive-confirmed` | 终态 |

**四个事件的副作用（来自 Comet 实现，不是本设计新增）**：

| 事件 | 副作用 |
|---|---|
| `archive-confirm` | 置 `archiveConfirmation = confirmed`；阶段不变 |
| `archive-reopen` | **`phase = 'verify'`**；清空 `verifyResult` / `verifiedAt` / `archiveConfirmation`，`verifyFailures = 0` |
| `preset-escalate` | 置 `workflow = full`（并把 `classicProfile` 同步为 `full`） |
| `verify-fail` | 阶段回 `build`（原生回流） |

> **`archive-reopen` 的落点是 `verify`，不是 `archive`**：本表按 Comet 的实现事实给出（`classic-transitions.ts` 的 `applyClassicTransition` 对 `archive-reopen` 设 `phase='verify'`）。分册 01 §23.3 `G-M7` 的"回 `verify` / `build`"中，**回 `verify`** 与本表一致，回 `build` 需经一次 `verify-fail`。


### 2.3 八个 Workflow Node 契约

| Node | kind | 原生 Skill | Output Schema | Guard |
|---|---|---|---|---|
| `open` | control | `comet-open` | `comet.intake.v1` | `.comet.yaml exists` |
| `design` | producer | `comet-design` | `comet.design.v1` | design artifacts（artifact-structured） |
| `plan` | producer | `comet-build` | `comet.plan.v1` | plan artifacts（artifact-structured） |
| `execute` | control | `comet-build` | `comet.execution-evidence.v1` | build evidence（semantic） |
| `subagent-execute` | handoff | `subagent-driven-development` | `comet.handoff.v1` | handoff evidence（evidence-only） |
| `review` | guardrail *(optional)* | `requesting-code-review` | `comet.review.v1` | review evidence（evidence-only） |
| `verify` | control | `comet-verify` | `comet.verify.v1` | verify result（evidence-only） |
| `archive` | control | `comet-archive` | `comet.archive.v1` | archive state（state-transition） |


### 2.4 四种 preset 路由

| preset | 阶段路径 | 跳过 |
|---|---|---|
| `full`（classic） | open → design → build → verify → archive | — |
| `tweak` | open → **build** → verify → archive | design、brainstorming、完整实施计划 |
| `hotfix` | open → **build** → 根因消除检查 → verify → archive | design、brainstorming、完整实施计划 |
| `any` | Skill 创建向导 | — |

`open-complete` 的目标阶段由 `workflow === 'full'` 决定：full 进 `design`，否则直接进 `build`。

### 2.5 产物逻辑根

| 逻辑根 | 说明 |
|---|---|
| `openSpecRoot` | OpenSpec 根。新项目为 `docs/openspec/`；legacy 为 `openspec/`。**随 `artifact_layout` 变化** |
| `changesRoot` | 未归档 change 根 = `<openSpecRoot>/changes` |
| `archiveRoot` | 归档 change 根 = `<openSpecRoot>/changes/archive` |
| `specsRoot` | 主 specs 根 = `<openSpecRoot>/specs` |
| `superpowersRoot` | **恒为 `docs/superpowers/`，不随 `artifact_layout` 变化**。含 `specs/`（Design Doc）、`plans/`（实施计划）、`reports/`（报告） |

**产物落哪个根由 Comet 的 `pathBase` 决定，不由"就近原则"决定**——见 §8.2 的权威表。注意 `design-doc` 与 `implementation-plan` 的 `pathBase` 是 `classic-superpowers-root`，即落在 `docs/superpowers/` 而非 `<openSpecRoot>`。

**路径必须使用逻辑根，不得写死目录。**

> **本仓库当前状态的偏差**：本仓库 `.comet/config.yaml` 声明 `classic.artifact_layout: docs`，但 `docs/openspec/` 已不存在，产物实际落在**根目录 `openspec/`**。这构成 Comet 布局协议所说的 **legacy / docs 两套根目录冲突**：
>
> > 若 root show 或任一写命令报告 legacy/docs 两套根目录冲突、配置无效或迁移未完成，立即停止写入，运行只读的 `comet doctor` 检查。不要自行扫描两个根目录后猜测 change 归属，也不要向两处同时写入。
>
> 处置：本设计**一律使用逻辑根**，不写死 `docs/openspec/`，因此设计本身不受该冲突影响；但 Phase 1 落地前必须先解决布局冲突（`comet doctor` → 确认后 `comet classic root move docs --apply`，或把配置补为 `legacy`）。另注：当前环境中 `comet` CLI 不在 `PATH` 上，上述命令需先安装 Comet 后才能执行。详见附录 C 的 `EXT-01` 与 `EXT-02`。

### 2.6 用户决策点协议（阻塞式）

- **用户决策点**：有两种或更多合法做法、且选择影响范围/行为/风险承担/不可逆结果时必须由用户决定；**阶段明确要求的产物与设计批准也属于决策点**
- **决策点是阻塞点**：必须暂停，等待用户明确选择后才能继续
- 不得用推荐规则、默认值、历史偏好或"用户应该会同意"的推断代替当前确认
- 用户明确选择前，不得写入对应状态字段、执行对应分支操作或自动继续下一阶段


`auto_transition` 只决定 guard 更新 `phase` 后**是否自动调用下一个 Skill**，不决定 phase 是否推进。

### 2.7 phase 与 node 的双轨关系（对草稿"六阶段"的澄清）

草稿文档把 build 阶段内的 `plan` 节点当成独立的"Build Plan 阶段"，又把 `archive` 称为 "Close"，从而得出一个不存在的"六阶段"。实际是两条轨道：
```text
phase（5，Comet 常量，不可改） node（8，Comet 原生契约，可按 preset 裁剪）
───────────────────────────── ──────────────────────────────────────────
open open
design design
build ────────────────────► plan
 execute / subagent-execute
 review
verify verify
archive archive
```

**设计规则**：
1. 说"阶段"时，**只指 5 个 phase**
2. 说"节点"时，指 8 个 Workflow Node
3. 草稿的 "Build Plan 阶段" → 一律表述为 **`plan` 节点**
4. 草稿的 "Close" → 一律表述为 **`archive` 阶段**
5. 草稿的 "Open 阶段（工程调查）" → 归入 **`design` 节点作为 Step 0**，理由见 §4.3

### 2.8 节点身份的持久化缺口

**本设计把 Agent 挂到 node 上（§4），但 Comet 当前并不持久化 node 身份。** 这是实施层必须先解决的缺口。

`ClassicState` 中**没有 `node` 字段**；可用的判别器只有：
| 已持久化字段 | 取值 | 判别能力 |
|---|---|---|
| `phase` | `open/design/build/verify/archive` | 只能定位到 `build` |
| `buildPause`（`build_pause`） | `['plan-ready']` | 区分「计划就绪待批」与「进入实施」 |
| `buildMode`（`build_mode`） | `subagent-driven-development` / `executing-plans` / `direct` | 区分实施策略 |
| `tddMode` / `reviewMode` / `verifyMode` | `tdd\|direct` / `off\|standard\|thorough` / `light\|full` | 纪律强度，非节点身份 |

**结论**：`phase` + `build_pause` + `build_mode` 只能区分 `plan` 与 `execute` 的**入口**，**无法区分 `subagent-execute`、`review`，也无法在 `build` 内稳定判定「写计划文件 vs 写源码」**。这直接影响不变量 I3 与 I7 的可执行性——Code Write Hook 没有节点身份就无法正确放行或拦截。

**本设计的裁定（本节为唯一权威表述）**：引入 **Node Projection**——**节点身份由 Comet Plugin 判定**（`get_current_node()`），**经 Comet Runtime 的状态写路径、与 `phase` 同一次事务落盘**到 `.comet/state/node.json`，记录当前 node id 与所属 phase；**Hook 只读该投影**，并在投影与 `phase` 不一致时 **fail-closed**。**权威分工**：`Plugin = 判定`、`Runtime = 写入`、`Hook = 只读`——三者缺一不可，不得简化为“由 Runtime 计算”。裁定见 `99 D60`，实现见分册 03 §1.1 与 §14.1 `IR-1`。

选择 **Runtime** 而非 Agent 作为权威计算方，理由：只有 Runtime 能保证投影与 `phase` 的原子一致性。若改由 Agent 自报节点，Code Write Hook 的放行依据就退化为"被审查方自己的声明"，不变量 I3 与 I7 将失去强制力。完整设计见分册 03。


---

## 3. 总体架构

### 3.1 六层架构

```text
┌─────────────────────────────────────────────────────────────┐
│ ① Human / Developer │
│ Requirement / Review / Approval / Decision │
├─────────────────────────────────────────────────────────────┤
│ ② Comet Lifecycle │
│ open → design → build → verify → archive │
├─────────────────────────────────────────────────────────────┤
│ ③ Agent Orchestration │
│ Main Agent / Stage Agents / Review Agents / Experts │
│ OMO Team Mode │
├─────────────────────────────────────────────────────────────┤
│ ④ Skills │
│ ATDD / Challenge / Test Design / Writing Plans / │
│ TDD / Code Review / Verification │
├─────────────────────────────────────────────────────────────┤
│ ⑤ Capability / Knowledge Layer │
│ CodeGraph / graphify / OpenViking / OpenWiki / Plugins │
├─────────────────────────────────────────────────────────────┤
│ ⑥ Enforcement / Automation │
│ Hooks / Validators / Evidence │
└─────────────────────────────────────────────────────────────┘
```


> **注**：架构稿另一处称"Hook 分成四层"（Stage / Policy / Execution / Evidence），那是 **Hook 的内部分类**，与本节的六层平台架构不是同一维度。

### 3.2 职责等式与分工冻结

```text
Comet = WHEN Agent = WHO
Skill = HOW Plugin = CAPABILITY
Hook = ENFORCEMENT CodeGraph = CODE FACTS
OpenViking = KNOWLEDGE Human = DECISION
Artifact = 状态载体 Traceability = 证据链
Human Gate = 最终决策
```

平台最终分工：
```text
Comet 生命周期编排 (Lifecycle Orchestration)
ATDD 需求行为契约 (Requirement Behavior Contract)
Design 架构契约 (Architecture Contract)
Test Design 验证契约 (Verification Contract)
Writing Plans 实施契约 (Implementation Contract)
TDD 实现纪律 (Implementation Discipline)
Verification 质量与证据契约 (Quality / Evidence Contract)
CodeGraph 代码事实 (Code Fact)
OpenViking 知识与记忆 (Knowledge / Memory)
OMO Agent 协作 (Agent Collaboration)
Plugins 能力 (Capability)
Hooks 强制 (Enforcement)
Human 决策 (Decision)
```

**硬原则**：
> **Plugin 可以提供能力，但不能绕过 Hook 的治理。**

例：Test Runner Plugin 可以执行测试，但不能自己决定"这个测试通过了，所以可以归档"。


### 3.3 平台最终形态与主数据流

```text
 Human
 │
 ▼
 Comet
 │
 ┌──────────┴──────────┐
 │ │
 Agents OMO
 │ │
 └──────────┬──────────┘
 ▼
 Skills
 │
 ┌──────────┼──────────┐
 ▼ ▼ ▼
 CodeGraph OpenViking Plugins
 │ │ │
 └──────────┼──────────┘
 ▼
 Hooks
 │
 ▼
 Firmware Repo
 │
 ▼
 Test / Build
 │
 ▼
 Evidence
 │
 ▼
 Verify
 │
 ▼
 Archive
```

阶段内部的数据流（以 `design` 为例）：
```text
Comet → design → OMO Team → Team Result → Design Agent → design.md
```


### 3.4 架构不变量（I1–I8）

以下 8 条是**架构级硬约束**，任何后续 Agent / Skill / Hook / Plugin / 产物设计不得违反。

| # | 不变量 | 违反示例 |
|---|---|---|
| **I1** | Human 拥有决策权，Agent 只提议 | Agent 自行批准 Scope 变更 |
| **I2** | 只有 Main Agent 持有生命周期状态；Agent 不得自决下一阶段 | Coding Agent 自行宣布进入 verify |
| **I3** | 代码修改只发生在 `build` 阶段的 `execute` / `subagent-execute` 节点，且必须 guard 通过 + 持有已批准 Build Task | 在 `design` 阶段直接改源码 |
| **I4** | 测试契约不得为获取 GREEN 而修改 | 改 Expected 让测试通过 |
| **I5** | CodeGraph 是事实来源，不是需求来源；知识与代码分离 | 用 CodeGraph 查询结果当作需求 |
| **I6** | 每个节点必须产出结构化 Artifact，否则 guard 无法校验、下游无法消费 | 只留一段对话式的"设计说明" |
| **I7** | Plugin 能力不得绕过 Hook 治理 | Test Plugin 自行判定通过并推进阶段 |
| **I8** | 失败必须先分类（四类）再修复 | 测试失败后直接改代码 |

---

## 4. 生命周期与节点挂载

本章把设计挂到 Comet 的真实节点上。每个节点给出：目标 / 输入 / 承担 Agent / Skill / 产物 / guard / Human Gate / 禁止项 / 回流。

### 4.0 总表

| 轨道 | 节点 | 承担 Agent | Skill | 产物 |
|---|---|---|---|---|
| Stage 0 | Requirement Decomposition | `ufs-requirements` | `prd-split` | `comet-artifacts/requirements/**` |
| Stage 0 | Challenge | `ufs-challenge` | `ufs-challenge` | `challenge-report.md` |
| phase open | `open` | `ufs-main` | `atdd-development` | OpenSpec `proposal.md` + `spec.md`（镜像） |
| phase design | `design` | `ufs-exploration`(Step 0) → `ufs-design` + `ufs-test-design` | `ufs-test-design` | `design.md` / Design Doc、`test-design.md` |
| phase build | `plan` | `ufs-build-plan` | `ufs-writing-plans` | `plans/*.md`、`tasks.md` |
| phase build | `execute` | `ufs-coding` | `ufs-tdd` | 代码 + TDD Evidence |
| phase build | `subagent-execute` | OMO / 子代理 | `subagent-driven-development` | `comet.handoff.v1` |
| phase build | `review` | `ufs-code-review` | `ufs-code-review`（特化 `requesting-code-review`） | `comet.review.v1` |
| phase verify | `verify` | `ufs-verification` | `ufs-verification` | `comet.verify.v1` |
| phase archive | `archive` | `ufs-main` + Human | `comet-archive` | `comet.archive.v1` |
| 门禁 | 各节点 | `ufs-document-review` | — | findings |
| 按需 | 全程 | `ufs-firmware-expert`、`ufs-failure-analysis` | — | 分析结论 / 失败分类 |

### 4.1 Stage 0：Requirement Decomposition 与 Challenge

**位置**：Comet 五阶段**之外**，早于任何 change 的存在。

**为什么不可能在 `open` 节点内**：`open` 节点的 guard 是 `.comet.yaml exists`，即进入 `open` 时 change 已创建；而 Stage 0 的产物目录 `comet-artifacts/requirements/` 与 OpenSpec change 目录**并列**，发生在 change 创建之前。这是位置关系的硬证据。

```text
Epic / PRD
 │
 ▼
Requirement Decomposition ──── ufs-requirements ──── comet-artifacts/requirements/
 │
 ▼ Human Review
grill-me Challenge ─────────── ufs-challenge
 │
 ▼ Human Story Freeze ←──── 离开 Stage 0 的唯一闸门
═══════════════ Comet phase 边界（此时才创建 change）═══════════════
```

| 项 | Stage 0-a | Stage 0-b |
|---|---|---|
| 目标 | Human Intent → 可审查的 ATDD 需求基线 | 对抗式质询需求本身的正确性、完整性、可验证性 |
| 输入 | PRD/Epic、项目 ATDD 规则、既有需求与决策 | 草稿 Story/AC/Scenario、原始 Requirement |
| Agent | `ufs-requirements` | `ufs-challenge` |
| Skill | `prd-split` | `ufs-challenge`（grill-me 规范化） |
| 产物 | `comet-artifacts/requirements/**` | `challenge-report.md` |
| 状态 | `NEEDS_HUMAN_DECISION` / `DRAFT_READY_FOR_CHALLENGE` / `BLOCKED` | `Verdict` + `Blocking/Non-blocking Findings` |
| Human Gate | Human Review | **Story Freeze** |

**禁止项**：Stage 0 不得写函数签名、表结构、算法、状态机细节、内存布局；不得把源码模块/文件/目录当作需求。

**回流**：`open` 发现需求冲突、或 `design` 无法覆盖 AC → 回流 Stage 0 重新 Challenge。

### 4.2 节点 `open`

| 项 | 内容 |
|---|---|
| 目标 | intake 用户请求、选择 change 形态、初始化 Comet state，并把 Frozen Story 镜像为 OpenSpec 产物 |
| 输入 | Stage 0 的 Frozen Story / AC / Scenario |
| 承担 Agent | `ufs-main`；门禁 `ufs-document-review` |
| Skill | `atdd-development` |
| 产物 | OpenSpec `proposal.md`、`specs/**/spec.md`（**均为镜像，非权威**）、`.comet.yaml` |
| Schema | `comet.intake.v1` |
| guard | `.comet.yaml exists`（state-transition） |
| Human Gate | 需求确认（Comet 原生）；AC 批准（方法论层） |
| 禁止项 | 不重新定义需求；不写实现方案 |

### 4.3 节点 `design`

| 项 | 内容 |
|---|---|
| 目标 | 把确认过的请求转成设计产物与 OpenSpec delta context |
| 输入 | Requirement / AC / AT / 既有架构 / CodeGraph / OpenViking |
| 承担 Agent | **Step 0**：`ufs-exploration`；**Step 1**：`ufs-design`；**Step 2**：`ufs-test-design` |
| Skill | `ufs-test-design` |
| 产物 | `design.md` / Design Doc（技术决策权威）、`test-design.md`（验证契约权威） |
| Schema | `comet.design.v1` |
| guard | design artifacts（artifact-structured） |
| Human Gate | 架构批准、验证策略批准 |
| 禁止项 | 不重定义用户可见行为；需求错误或不完整时 `STOP` 并报 `DESIGN_BLOCKER`，不得静默重新解释 |

**为什么"工程调查"放在这里而不是 `open`**：`design` 节点的责任是"把已确认的请求转成设计产物"，而 Comet 原生 `design` 的输入本就包含既有架构与 CodeGraph；Comet 的 `open` 则在语义上是 intake + 确认 + 初始化 state，不承担影响分析。因此草稿所称的"Open 阶段（工程调查）"归入 `design` 节点的 Step 0，由 `ufs-exploration` 执行，产出 Exploration Report 作为 Design 的输入。

### 4.4 节点 `plan`

| 项 | 内容 |
|---|---|
| 目标 | 把 Design 转成可执行的工程修改计划与任务契约 |
| 输入 | Requirement / AC / AT / Design / Test Design / CodeGraph / 既有固件 |
| 承担 Agent | `ufs-build-plan` |
| Skill | `ufs-writing-plans` |
| 产物 | `plans/*.md`（实施计划）、`tasks.md`（**任务状态权威**） |
| Schema | `comet.plan.v1` |
| guard | plan artifacts（artifact-structured） |
| Human Gate | Build Plan 批准 |
| Task 规则 | 一个 Build Task 必须代表一个**工程实现步骤**；**不得把每个 UT case 直接转成一个 Build Task** |

核心输出契约字段：**字段集的唯一定义处是分册 02 §9.6.3**（含 ★ 扩展字段与消费声明）；本节**不重复**字段清单，避免出现第二套权威。要点：

- 写入范围字段是 **`scope.files` / `scope.functions`**（`H04`/`H05` 的唯一读取字段）；
- 每个 Build Task 必须写 `verification_level` 与 `required_regression_level`，并声明 `verification_strategy_consumed` / `non_testable_consumed`；
- 字段名与方法论视图（分册 01 §12.5）**逐字一致**，缺任一 ★ 字段即 `H03` `BLOCK`。


### 4.5 节点 `execute` 与 `subagent-execute`

| 项 | 内容 |
|---|---|
| 目标 | 按已批准计划实施；这是**唯一允许修改代码**的位置 |
| 输入 | 已批准 Build Task、Design、Test Contract |
| 承担 Agent | `ufs-coding`（execute）；OMO / 子代理（subagent-execute） |
| Skill | `ufs-tdd`；派发用 `subagent-driven-development` |
| 产物 | Code / Test / Regression Result / TDD Evidence / Implementation Notes |
| Schema | `comet.execution-evidence.v1` / `comet.handoff.v1` |
| guard | build evidence（semantic）/ handoff evidence（evidence-only） |
| 原则 | **一次只实现一个已批准 Build Task** |

**TDD 循环**：`Build Task → Test Preparation → RED → Implementation → GREEN → REFACTOR → Regression → Evidence`

**Legacy 路径**：`CodeGraph → Existing Behavior → Characterization Test → Baseline GREEN → New Behavioral Test → RED → Implementation`

**禁止项**：为通过测试而修改 AC；为取得 GREEN 而修改预期行为；静默修改已批准 Design；扩大 Scope；修无关缺陷；自行修改 Test Contract。

**失败处理**：测试失败时**不要立即改代码**，先分类 `TEST_PROBLEM / CODE_PROBLEM / DESIGN_PROBLEM / ENVIRONMENT_PROBLEM` 并按类路由。


### 4.6 节点 `review`

| 项 | 内容 |
|---|---|
| 目标 | 在 verify 之前独立核查实现（Comet 原生 `guardrail` 节点，`optional: true`） |
| 输入 | Git Diff、测试代码、已批准 Design、Build Plan、Test Contract |
| 承担 Agent | `ufs-code-review` |
| Skill | `ufs-code-review` —— **UFS 特化实现，取代通用 `requesting-code-review`** |
| 产物 | `comet.review.v1`（review summary + blockers） |
| guard | review evidence（evidence-only） |
| 独立性规则 | 与被审代码的编写者**不得是同一执行实例**；不改被审代码，只返回结论 + findings |

**这是 Comet 原生已解决的闸门**。草稿文档把它漏成"无归属"，本设计据 Comet 契约恢复其位置，并注入 UFS 固件特有审查维度（中断上下文、寄存器访问、DMA 一致性、时序假设）。


### 4.7 节点 `verify`

| 项 | 内容 |
|---|---|
| 目标 | 验证**整条链**，而非执行一次 `pytest` |
| 输入 | Requirement / AC / AT / Design / Test Design / Build Plan / Git Diff / Test·Regression Result / **Code Review** / Evidence |
| 承担 Agent | `ufs-verification` |
| Skill | `ufs-verification` |
| 产物 | **`comet.verify.v1` 本身不是文件**：`artifacts: []`，evidence 为 `verification-commands` / `result`，由 Runtime 从真实文件推导（§2.3、§8.2）。**本设计在其之上冻结 verdict 闭集** `READY_FOR_HUMAN_ACCEPTANCE` / `NEEDS_REVISION` / `BLOCKED`（本设计新增口径，字段契约见分册 02 §12.6；不得据此创建 `verify.json`） |
| guard | verify result（evidence-only） |
| 原则 | **Tests passing alone is insufficient** |
| 回流 | `verify-fail` → **build**（Comet 原生迁移） |

验证的链条：
```text
Requirement → Acceptance → Design → Test Design → Build Plan → Code Change → Test Result → Evidence
```

Verify Agent 必须回答 12 问：需求是否全覆盖 / AC 是否全覆盖 / AT 是否执行 / Verification Level 是否合理 / UT·Integration·Simulator·HW 是否选择正确 / Test Result 是否可信 / Code 是否符合 Design / Code 是否符合 Build Plan / 是否存在未批准 Scope Change / 是否存在未解决问题 / Evidence 是否完整 / 是否满足归档条件。


### 4.8 节点 `archive`

归档**不是**"测试通过"。

**归档条件（全部满足）**：
```text
Requirement Covered ∧ Acceptance Covered ∧ Verification Complete
∧ Code Review Complete ∧ Evidence Complete ∧ No Blocking Issue
∧ Human Accepted
```

| 项 | 内容 |
|---|---|
| 目标 | 把已验收的行为契约与验证证据固化为可离线复核的归档（**归档 ≠ commit**） |
| 输入 | `verify` 的结论（`READY_FOR_HUMAN_ACCEPTANCE`）、四职能审计副本与汇总索引、`.comet/approvals/**`、Human 的接受决定 |
| 承担 Agent | `ufs-main` 组装 + **Human 决策** |
| Skill | `comet-archive` |
| 产物 | **`comet.archive.v1` 本身不是文件**：`artifacts: []`，evidence 仅 `archive-summary` / `archived-state`，由 Runtime 推导（§2.3、§8.2）。“Verification Report / Traceability Report / Evidence Index / Change Summary / Known Issues / Final Decision”是**本设计在 evidence 之上的汇总口径，不是六个产物文件**；其唯一落盘形态是**归档汇总索引** `comet-artifacts/evidence/<change>/index.md`（四职能全入，`99 D44`）与审计副本目录（分册 03 §9.1–§9.5、§14.1 `IR-14`） |
| guard | **`archive state`（`state-transition`）**——Comet 原生 node guardrail（§2.3 为唯一权威）。另有**迁移表事件** `verify-pass` / `archive-confirm`（§2.2）；两者不同源，不得混用 |
| 禁止项 | 仅因测试通过就归档；带 Blocking Issue 归档 |


### 4.9 preset 裁剪矩阵

新设计的 Agent 与 Skill 必须在三种 preset 下都能正确降级，否则会破坏 Comet 既有流程。

| 能力 | `full` | `tweak` | `hotfix` |
|---|---|---|---|
| Stage 0 需求分解 | ✅ 完整 | ⚠️ 轻量：仅 Story 边界与 AC | ⚠️ 跳过：缺陷已有既有行为 |
| Stage 0 Challenge | ✅ | ⚠️ 仅 AC 可验证性 | ❌ 跳过 |
| `open` | ✅ | ✅ | ✅ |
| `design` / `ufs-test-design` | ✅ 完整 | ❌ 跳过（open → build） | ❌ 跳过 |
| `plan` / `ufs-writing-plans` | ✅ 完整计划 | ⚠️ 简化任务清单 | ⚠️ 简化任务清单 |
| `execute` / `ufs-tdd` | ✅ RED→GREEN→REFACTOR | ✅ 但可省 REFACTOR 证据 | ✅ + **根因消除检查** |
| `review` | ✅ | ✅ | ✅ |
| `verify` | ✅ | ✅ | ✅ |
| `archive` | ✅ | ✅ | ✅ |
| `preset-escalate` | — | 触发条件见架构稿 | 触发条件见架构稿 |

**硬约束**（事实来自 Comet 的 preset 硬编码默认值，见分册 03 §13.3 与本设计 `98` A11 的源级复核；**不由 `99 D62` 承载**）：Comet 对 preset 有**硬编码默认值**——`hotfix` / `tweak` 默认 `tdd_mode=direct`、`review_mode=off`。上表描述的是 **UFS 要求的取值**，因此在这两个 preset 下**必须在 `open` 节点初始化时显式覆盖**：`tweak` → `tdd_mode=tdd`；`tweak` / `hotfix` → `review_mode=standard`（UFS 要求三 preset 均执行 `review`）。**未显式覆盖即视为未配置**：不得以“Comet 默认就是 off”为由跳过 `review`，也不得在 `tdd_mode=direct` 下声称完成 TDD。

**关键约束**：`hotfix` 与 `tweak` 会跳过 `design` 节点，因此这两个 preset 下 **`ufs-test-design` 不运行**。这意味着 Verification Level 的判定必须在 `plan` 节点内以轻量形式补做，否则固件代码会在未判定验证层级的情况下直接进入 TDD。

---

## 5. 统一追溯模型

### 5.1 ID 体系

**本节是 ID 与命名空间的唯一权威。** 分册只能引用，不得另行增删前缀、改变段数，或把非对象编号登记为对象 ID。Hook 的 ID 形态断言（`03 §7.3.7 H13`）以 §5.1.1 的五张子表为唯一输入。

| 对象 | 规范示例 | 产生位置 |
|---|---|---|
| Requirement | `REQ-001` | Stage 0 |
| Decision | `DEC-001` | Stage 0 / Human |
| Assumption | `ASM-001` | Stage 0 |
| Epic | `EPIC-001` | Stage 0 |
| Capability | `CAP-001` | Stage 0 |
| Story | `STORY-001` | Stage 0 |
| Acceptance Criteria | `AC-001-01` | Stage 0 |
| Scenario | `SC-001-01` | Stage 0 |
| Acceptance Test | `AT-001-01` | `open` |
| **Behavior** | **`BEH-001-01`** | `design`（行为清单；全链引用锚点） |
| Design Item | `DES-001-01` | `design` |
| Test Case | **`TC-001-01`**（统一；层级由 `level` 属性承载） | `design`（行为级）→ `plan`（映射级）→ `execute`（可执行级） |
| Implementation Item | `IMP-001-01` | `plan` |
| Review Record | `RVW-001-01` | `review` |
| Review Finding | `REV-001-01` | `review` |
| Evidence | `EVD-001-01` | `execute` / `verify` |

**`BEH-`（Behavior）**：`design` 阶段识别的**可独立验证的系统行为**，是「行为 → 验证层级 → 回归等级 → 测试用例 → Build Task」整条链的引用锚点。`verification_strategy` 的每一项、每个 Build Task 的验证层级、以及 UT 闭包检查都以 `BEH-*` 为枚举域（见分册 01 §9）。

**`TC-`（Test Case）**：**七级验证层级的测试用例共用此前缀**，具体层级由该用例的 `level` 属性承载（`UNIT`/`COMPONENT`/`INTEGRATION`/`SIMULATOR`/`HARDWARE`/`INSPECTION`/`MANUAL`）。`UT-` 与 `IT-` 是 `TC-` 在 `UNIT` / `INTEGRATION` 层级的**历史别名**，既有产物可继续解析；**新产物一律用 `TC-`**。理由：前缀命名**对象**（Test Case），层级是属性——避免为每一级新增前缀，也与"域标签不进 ID"的原则一致。

**`RVW-` / `REV-`（Review）**：一轮审查的**记录**用 `RVW-`（承载于 `comet.review.v1.review_id`）；该轮审查产出的**条目**用 `REV-`（承载于 `findings[].id`，`blockers[]` 引用 `REV-*`）。二者是不同对象，故使用不同前缀，不得混用。

**ID 规范化规则**：
1. **统一为「对象前缀 - Story 号 - 序号」两段式**；域标签（如 `FC`、`ST`）只允许出现在**名称**中，不出现在 ID 中。理由：ID 必须可被 Hook 机器解析，域标签是自由文本。

2. **废弃形式表**（下列形式**不得**作为当前 ID 使用）：
| 废弃形式 | 规范形式 | 说明 |
|---|---|---|
| `BUILD-FC-002`、`BUILD-001-01` | `IMP-001-02` / `IMP-001-01` | `BUILD-` 前缀作废；实现项用 `IMP-` |
| `CHG-FC-001` | 无 ID（变更以 change 名标识） | `CHG-` 不属 ID 体系 |
| `UT-FC-001`、`UT-ST-001` | `TC-001-01` | 去域标签；状态序列用例用名称区分（`UT-` 现为 `TC-` 在 `UNIT` 层的别名） |
| `AT-FC-001` | `AT-001-01` | 去域标签 |
| `IT-FC-001` | `TC-001-01` | 去域标签（`IT-` 现为 `TC-` 在 `INTEGRATION` 层的别名） |
| `CHAR-001-01` | `TC-001-09`（`case_type: characterization`） | 表征测试是 Test Case 的**类型**，不是独立对象 |
| `TESTABILITY-FC-001`、`TEST-DESIGN-FC-001`、`VERIFY-FC-001` | 无独立 ID | 这些不是对象，是可测性/设计/验证**属性**，归入所属 `DES-` / `TC-` / `EVD-` |
| `AC-001`、`SC-001`（单段式） | `AC-001-01`、`SC-001-01` | 必须两段式 |

3. **迁移期**：历史文档/既有 change 中的废弃形式按"只读不改 + 一次性索引映射"处理（`99 D17`），新产物一律用规范形式。

#### 5.1.1 命名空间总表（Hook 判定基准）

**任何一个 `PREFIX-` 形态必须且只能落在下列五张子表之一。** 表 N1/N2/N3 是**对象 ID**（构成追溯主干 `REQ → … → EVD`）；表 N4/N5 不是对象 ID，**不得**出现在追溯主干中。

**表 N1：合法单段集（`PREFIX-nnn`；对象没有 Story 维度）**

| 前缀 | 对象 | 产生位置 |
|---|---|---|
| `REQ` | Requirement | Stage 0 |
| `DEC` | Decision | Stage 0 / Human |
| `ASM` | Assumption | Stage 0 |
| `EPIC` | Epic | Stage 0 |
| `CAP` | Capability | Stage 0 |
| `STORY` | Story | Stage 0 |

**表 N2：合法两段集（`PREFIX-STORY-SEQ`）**

| 前缀 | 对象 | 产生位置 |
|---|---|---|
| `AC` | Acceptance Criteria | Stage 0 |
| `SC` | Scenario | Stage 0 |
| `AT` | Acceptance Test | `open` |
| `BEH` | Behavior（行为清单；全链引用锚点） | `design` |
| `DES` | Design Item | `design` |
| `TC` | Test Case（七级共用；层级由 `level` 承载） | `design` → `plan` → `execute` |
| `IMP` | Implementation Item / Build Task | `plan` |
| `RVW` | Review Record（一轮审查的记录） | `review` |
| `REV` | Review Finding（`findings[].id`） | `review` |
| `EVD` | Evidence | `execute` / `verify` |

**表 N3：历史别名集（只读解析；不得新写）**

| 别名形态 | 规范形式 | 说明 |
|---|---|---|
| `UT-nnn-nn` | `TC-nnn-nn`（`level: UNIT`） | 单元层历史别名 |
| `IT-nnn-nn` | `TC-nnn-nn`（`level: INTEGRATION`） | 集成层历史别名 |
| `CHAR-nnn-nn` | `TC-nnn-nn`（`case_type: characterization`） | 表征测试不是独立对象 |
| `BUILD-nnn-nn` | `IMP-nnn-nn` | 实现项旧前缀 |
| `CHG-…` | 无 ID（变更以 change 名标识） | `CHG-` 不属 ID 体系 |
| 带域标签形态（`XXX-FC-nnn`、`XXX-ST-nnn`） | 去域标签后的规范形式 | 域标签只允许出现在名称中 |

历史别名与废弃形式的完整对照见 §5.1 废弃形式表与分册 02 §1.3。

**表 N4：规则与事项命名空间集（非对象 ID；不进追溯主干；H13 不检查）**

| 前缀 | 含义 | 唯一权威 |
|---|---|---|
| `FR-1`…`FR-5` | 五条正式规则（Formal Rules） | 分册 01 §28 |
| `SYS-1`…`SYS-7` | 跨 Skill 体系级硬规则 | 分册 02 §3.1 |
| `SR-1`…`SR-7` | Skill 侧附加规则 | 分册 02（各 Skill 的 Hard Rules） |
| `ACR-1`…`ACR-6` | AC 质量规则 | 分册 01 §5.1 |
| `TD-*` | Test Design 硬规则 | 分册 02 §8.10 |
| `TDD-*` | TDD 模式硬规则 | 分册 02 §10.10 |
| `CR-1`…`CR-10` | Code Review 硬规则 | 分册 02 §11.10 |
| `WP-*` | Build Plan 工作原则 | 分册 02 §9.10 |
| `TCR-1`…`TCR-4` | Test Contract 规则 | 分册 03 §6.6 |
| `ML-*` | 映射层级硬规则 | 分册 01 §21.3 |
| `RG-*` | 回归分级规则 | 分册 01 §10.4 |
| `TCP-*` | Test Contract Protection 规则 | 分册 01 §18.3 |
| `MK-*` | Mock 纪律 | 分册 01 §20.4 |
| `TDATA-*` | 测试数据规则 | 分册 01 §20.6 |
| `NG-*` | Native 适配缺口 | 分册 03 §4.2 |
| `MR-1`…`MR-13` | 分册 01 本册裁定 | 分册 01 §30 |
| `SKR-1`…`SKR-15` | 分册 02 本册裁定 | 分册 02 §13.1 |
| `IR-1`…`IR-14` | 分册 03 本册裁定 | 分册 03 §14.1 |
| `S0R-1`…`S0R-20` | 分册 04 本册裁定 | 分册 04 §7.1 |
| `D01`…`D86` | 全局裁定 | `99` §1 |
| `EXT-01`…`EXT-13` | 外部依赖事项（全文档集唯一编号） | 本册附录 C |

**规则编号的层级关系与语义消歧由 §5.1.2 唯一持有**；表 N4 只固定"前缀归属谁"，不重复定义内容。

**表 N5：局部命名空间排除集（报告内/章节内局部；生命周期短，或仅作历史索引；永不进入追溯主干）**

| 前缀 | 含义 | 存放位置 | 关闭动作 |
|---|---|---|---|
| `BLOCK-nnn` | Challenge 报告的阻塞性 Finding | `challenge-report.md` | 必须被处置后才可冻结 |
| `WARN-nnn` | Challenge 报告的非阻塞 Finding | `challenge-report.md` | 记录或显式接受 |
| `Q-nnn` | Challenge 报告的待人类回答问题 | `challenge-report.md` | 关闭后升级为 `DEC-nnn` 或 `ASM-nnn` |
| `CHAL-nnn` | Challenge 报告自身编号（`report_id`） | `challenge-report.md` | 随报告归档 |
| `P-#` | `prd-split` Skill 的章节内规则条目 | 分册 02 §5.10 | 不适用（章节内编号） |
| `C-#` | `ufs-challenge` Skill 的章节内规则条目 | 分册 02 §6.10 | 同上 |
| `V-#` | `ufs-verification` Skill 的章节内规则条目 | 分册 02 §12.10 | 同上 |
| `U-#` | `03 §0.2`「上游契约与坐标系」的条目编号（`U1`…`U8`） | 分册 03 §0.2 | 不适用（章节内编号） |
| `O#`（无连字符） | 分册 02 §4.3「结构化输出三条最低要求」（`O1`–`O3`） | 分册 02 §4.3 | 不适用（章节内编号） |
| `O-#` / `E-#` / `H-#` / `A-#` | `03` 各节点"原生 vs UFS 差异"表的行编号（`open` / `execute` / `subagent-execute` / `archive`） | 分册 03 §4.2 / §5.x / §9.x / §11.x | 同上 |
| `C##` / `R#` | `99` 的覆盖项 / 残余风险编号 | `99` §3 / §4 | 不适用（册内编号） |
| `G1`–`G5` / `NT-1`–`NT-9` / `S1`–`S5` | 主文档 §1.3 设计目标 / §1.4 非目标 / §1.5 成功判据 | 主文档 §1 | 不适用（册内编号） |
| `P1`–`P5`（§2.1 失效模式）/ `P1`–`P4`（§6.5 保护）/ `P1`–`P10`（分册 01 §2.1 原则） | **同符号在不同节表示不同集合——引用时必须带节号**（如“§6.5 的 P3”） | 主文档 §2.1/§6.5；分册 01 §2.1 | 不适用（册内编号） |
| `T1`–`T16` | 分册 01 §1.2 术语基线条目 | 分册 01 §1.2 | 不适用（册内编号） |
| `B1`–`B3`（分册 04 §1.2 边界规则）/ `F1`–`F13`（分册 04 §2.4 禁止清单） | 分册 04 的章内条目 | 分册 04 §1.2 / §2.4 | 不适用（册内编号） |
| `G#` / `N-##` / `A-##` / `O#` / `D-#` / `X-#` / `BLOCKER-#` | 归并前的历史编码 → 现编号对照 | `99` §5 | 对照完成后仅作历史索引 |

**易混提示（H13 必须区分）**：Hook 编号形如 `H01`…`H13`（**无连字符**），与上表 `H-#`（章节内行号）不是同一空间；**边界值记号**（如 `Critical-1` / `Critical+1` / `Recovery-1`）与"名称 + 数字后缀"的自由文本不是 ID，H13 只对 `ID 字段` 与 `追溯主干引用` 做断言，不扫正文里的取值记号；`C##`（`99` 覆盖项）、`C-#`（`02 §6.10` 行号）、`CR-#`（规则编号）三者互不相同；`E-#`（`03 §5.x` 行号）与 `EXT-##`（外部依赖事项）互不相同。

**判定规则（H13 内联）**：

1. 形态必须是 `<前缀>-<序号>`（单段）或 `<前缀>-<序号>-<序号>`（两段），且**前缀大小写敏感**；
2. **单段**形态的前缀必须 ∈ 表 N1，**两段**形态的前缀必须 ∈ 表 N2；表 N3 形态只在"历史产物只读解析"路径被接受，**新建产物中一律违规**；
3. ID 中**不得**出现域标签（`FC`、`ST` 及其它 2–3 位全大写域码）；
4. 追溯主干中的每一处引用**不得**使用表 N4/N5 的前缀；表 N5 的编号一旦出现在主干引用中即违规。

#### 5.1.2 三套编号体系的消歧（防止同号不同义）

源文档与草稿中存在**三套都用 "Rule 1..N" 编号**的规则集，指向完全不同的内容。本设计强制使用不同前缀，**禁止裸用 "Rule N"**：
| 前缀 | 名称 | 归属 | 内容 |
|---|---|---|---|
| **`FR-1` … `FR-5`** | **五条正式规则（Formal Rules）** | 分册 01 §28（**唯一权威文本**） | Acceptance Traceability / Verification Level / Legacy TDD / Implementation Independence / Controlled Failure Loop |
| **`SR-1` … `SR-7`** | **Skill 侧附加规则（Skill Rules）** | 分册 02，各 Skill 的 Hard Rules | No hidden assumptions / Behavior before implementation / No function binding / No self-defined requirements / No test-contract manipulation / Legacy behavior must be distinguished / Every requirement needs verification intent |
| **`SYS-1` … `SYS-7`** | **跨 Skill 体系级硬规则** | 分册 02 §3 | 多份 Skill 重复声明的体系级约束 |

**层级关系（已裁定）**：`FR-*` ⊃ `SYS-*` ⊃ `SR-*` ⊃ Skill Steps。即：正式规则是全局强制条款；体系级规则是跨 Skill 的共同约束；Skill 侧规则是该 Skill 内的操作约束；Skill 的执行步骤位于最底层。**三者互不替代**，同一条内容不得在三处重复定义。

**冲突消解顺序固定为 `FR-*` → `SYS-*` → `SR-*` → Skill Steps**（上位优先）；**仅在同一个层级内部**，才按"更具体者优先"处理。完整裁定见 [`99-共识裁定表.md`](99-共识裁定表.md) D14。

参考资料中被提议的两条附加原则（Test-First Design、Legacy Characterization）**不占用 `FR-*` 编号**——参考资料明确把正式规则的数量固定为五条，把两条原则并入会改变该计数。二者已作为**平台设计原则**落入 §3.4 的架构不变量与 §14 的设计原则。

#### 5.1.3 Verification Level 与 Regression Level 的拼写冻结

- **Verification Level（七级，全大写）**：`UNIT` / `COMPONENT` / `INTEGRATION` / `SIMULATOR` / `HARDWARE` / `INSPECTION` / `MANUAL`。**不得写作 `UT`**——`UT` 只是"单元测试"这一用例的**历史别名**（表 N3），不是层级名，也不得作为新产物的前缀。
- **Regression Level**：`L0` … `L5`。
- 两者的**正交映射表由分册 01 §10 唯一持有**，其他分册只能引用，不得自算数值。

### 5.2 最低追溯要求

```text
Code Change → STORY → AC → DES
Test → AC
Evidence → TEST → AC
```

任何代码、设计或测试无法关联 Story / AC，都应**进入审查**，而不是默认视为合理工作。

### 5.3 完整追溯脊柱

```text
REQ → Capability → Story → AC → Scenario → Engineering Boundary
 → Design → Implementation → Test → Evidence → Archive
```

### 5.4 无 AC 对应的 UT 如何处置

允许 UT 来源于 `Design Constraint`、`Robustness` 等**非直接 Acceptance Requirement**。此类 UT 必须显式标注其来源类型与理由，并进入审查；**不得静默存在，也不得因此反向伪造 AC**。

---

## 6. Agent 架构

### 6.1 名册总览（11 + 2）

架构稿的原始模型是 `1 主控 + 7 阶段 + 2 专家`。经与 Comet 节点契约对齐后，本设计按**职能层**重新分类：
```text
Comet Runtime 内（11）
 ┌ 编排 1 ufs-main
 ├ 执行 5 ufs-exploration, ufs-design, ufs-test-design,
 │ ufs-build-plan, ufs-coding
 ├ 审查 3 ufs-document-review, ufs-code-review, ufs-verification
 └ 专家 2 ufs-firmware-expert, ufs-failure-analysis

Stage 0 前置（2，在 Comet phase 之外）
 ufs-requirements, ufs-challenge
```

**受控性质**：**只有 `ufs-main` 持有生命周期状态**；其余 12 个都是被分派的窄职责 subagent；2 个专家按需挂载，不占节点槽位。

架构稿的"7 阶段 Agent"把 `ufs-document-review` 计入阶段执行者，这是其计数别扭的根因——文档审查是**横切**职能，不进生命周期链。本设计将其归入"审查"层。

### 6.2 契约总表

| Agent | mode/temp | Role | 关键输入 | 主要输出 | 可用工具 |
|---|---|---|---|---|---|
| `ufs-requirements` | primary / 0.1 | Stage 0 需求分解：Human Intent → ATDD 需求基线 | PRD/Epic、既有需求与决策 | `comet-artifacts/requirements/**` | read / glob / grep / edit(限需求目录) / skill `prd-split` |
| `ufs-challenge` | subagent / 0.0 | Stage 0 对抗式质询需求本身 | 草稿 Story/AC/Scenario、原始 Requirement | `challenge-report.md` | read / glob / grep / edit(限 challenge 目录) |
| `ufs-main` | primary / 0.1 | 生命周期编排、分派、Gate 触发、追溯维护 | 用户请求、既有 artifacts | 阶段推进、`comet.intake.v1` | 经 Plugin 间接使用受控能力 |
| `ufs-exploration` | subagent / 0.1 | 调查既有固件，建立既有实现与行为的**事实** | User Story、既有固件与文档 | `<superpowersRoot>/reports/exploration-report.md` | CodeGraph:READ、graphify:READ、OpenViking:READ、Git:READ |
| `ufs-design` | subagent / 0.1 | 把已批准行为需求转成实现设计 | Requirement、AC、AT、Exploration Report、既有架构 | `design.md` | CodeGraph、OpenViking |
| `ufs-test-design` | subagent / 0.0 | 验证架构：决定已设计行为如何被验证 | AC、AT、Design、既有测试 | `test-design.md` | CodeGraph、OpenViking |
| `ufs-build-plan` | subagent / 0.1 | 创建可执行实现计划与 Implementation DAG | Approved Design、Test Design、CodeGraph | `plans/*.md`、`tasks.md` | CodeGraph、OpenViking |
| `ufs-coding` | subagent / 0.1 | 一次实现一个已批准 Build Task | 已批准 Build Task / Design / Test Contract | Code、Test、Regression、TDD Evidence | CodeGraph、OpenViking、Code Write、Test、Git(受控) |
| `ufs-code-review` | subagent / 0.0 | 在 verify 前独立核查实现 | Git Diff、测试代码、Design、Build Plan、Test Contract | `comet.review.v1` | CodeGraph:READ、Git:READ |
| `ufs-verification` | subagent / 0.0 | 独立验证变更是否满足已批准契约 | 契约全集、Git Diff、Test Result、**Code Review** | `comet.verify.v1` | CodeGraph、OpenViking、Test、Git(只读) |
| `ufs-document-review` | subagent / 0.0 | 独立工程文档评审（**不审代码**） | 其他 Agent 的 artifacts | `PASS` / `NEEDS_REVISION` + findings | CodeGraph:READ、OpenViking:READ |
| `ufs-firmware-expert` | subagent / 0.1 | 提供领域技术事实与分析 | 领域问题、知识源 | `FACT/REFERENCE/ANALYSIS/ASSUMPTION` 分层分析 | CodeGraph、OpenViking、OpenWiki、内部文档 |
| `ufs-failure-analysis` | subagent / 0.0 | 在纠正动作**之前**分析失败并分类 | Failure Log、Test Case、Code Diff、Design、Plan | 分类 + 证据 + 根因假设 + 置信度 + 建议动作 | CodeGraph、OpenViking、只读 |


### 6.3 四个审查/验证职能的职责划分

§6.1 把 `ufs-document-review`、`ufs-code-review`、`ufs-verification` 归入"审查 3"，另在 Stage 0 前置中设 `ufs-challenge`。**四者互不重合**，这是本设计的关键分工：
| 职能 | 问的问题 | 姿态 | 时机 | 对象 | 独立性规则 |
|---|---|---|---|---|---|
| `ufs-challenge` | 这个需求**对不对**？完整吗？可验证吗？ | **对抗式** | Stage 0 / Story Freeze 前 | 草稿 Story / AC / Scenario | 不改被审对象；Blocking Finding 阻断 Freeze |
| `ufs-document-review` | 这些制品之间**自洽吗**？可追溯吗？ | **一致性** | 各节点门禁 | 各阶段已产出制品 | 不改被审文档；`PASS` / `NEEDS_REVISION` |
| `ufs-code-review` | 这段代码**对不对**？符合设计与计划吗？ | **技术核查** | `review` 节点 | C diff + 测试代码 | 与代码编写者不得是同一执行实例 |
| `ufs-verification` | **整条契约链**是否真正被满足？ | **独立验收** | `verify` 节点 | 契约全集 + Git Diff + Test Result + Code Review | 必须独立于 Build 的局部成功；`Tests passing alone is insufficient` |

区别要点：`ufs-code-review` 看**代码本身**，`ufs-verification` 看**代码到契约的整条链**；前者的结论是后者的一项输入。

**跨职能独立性的约束只有两条**（与上表一致）：① `ufs-code-review` 不与代码作者同执行实例；② `ufs-verification` 独立于 Build 的局部成功。不设"四者互斥"一类的全局约束。

### 6.4 各 Agent 七段契约

每个 Agent 的 `Role / Inputs / Responsibilities / Hard Rules / Authority / Forbidden / Output` 完整定义见分册 02 与 03；本册只冻结**边界**：
- `ufs-main` **不能绕过生命周期规则直接修改代码**；Main 不等于万能 Agent
- `ufs-exploration` 禁止修改源码、测试、需求、AC、已批准设计
- `ufs-design` 不得重定义用户可见行为；需求有问题时报 `DESIGN_BLOCKER`
- `ufs-coding` 是**唯一可写源码的 Agent**；一次只做一个已批准 Build Task
- `ufs-verification` 必须尽可能避免"自己写代码 → 自己说代码正确"
- `ufs-document-review` 禁止批准自己修改过的文档
- `ufs-firmware-expert` 绝不把假设当作事实呈现；区分 `FACT / REFERENCE / ANALYSIS / ASSUMPTION`
- `ufs-failure-analysis` 不得直接修改源代码；只分类与路由


### 6.5 权限模型（13 行矩阵）

架构稿的权限矩阵只有 9 行，**缺 `ufs-failure-analysis`**，且未覆盖本设计新增的 `ufs-requirements`、`ufs-challenge`、`ufs-code-review` 与 Stage 0。本设计补全为 13 行。

**范本**：`prd-split` 已经实现了正确的权限模型——**默认全拒 + 单写路径白名单**，本设计的权限模型直接沿用该模式。

```yaml
permissions:
 - {action: "*", resource: "*", effect: deny}
 - {action: read, resource: "*", effect: allow}
 - {action: glob, resource: "*", effect: allow}
 - {action: grep, resource: "*", effect: allow}
 - {action: edit, resource: "comet-artifacts/requirements/**", effect: allow}
 - {action: skill, resource: "prd-split", effect: allow}
```

> **写路径粒度以分册 03 §8.4 的 glob 表为准**；本节给形态与归属。**evidence 类记录（`comet.review.v1` / `comet.verify.v1` / `comet.archive.v1`）不是文件**，由 Runtime 推导，**任何 Agent 都不写**。

| Agent | 读 | **写（唯一允许路径）** | 执行 | Skill |
|---|---|---|---|---|
| `ufs-requirements` | 全仓 | `comet-artifacts/requirements/**` | — | `prd-split` |
| `ufs-challenge` | 全仓 | `comet-artifacts/challenge/**` | — | `ufs-challenge` |
| `ufs-main` | 全仓 | `<openSpecRoot>/changes/<change>/.comet.yaml`（经 CLI）、`<archiveRoot>/**`、`comet-artifacts/evidence/<change>/index.md`（汇总） | Comet CLI | 全部 |
| `ufs-exploration` | 全仓 | `<superpowersRoot>/reports/exploration-report.md` | CodeGraph / graphify / OpenViking 查询 | — |
| `ufs-design` | 全仓 | `<superpowersRoot>/specs/*.md`（Canonical Design Doc） | — | — |
| `ufs-test-design` | 全仓 | `<superpowersRoot>/specs/test-design.md` | — | `ufs-test-design` |
| `ufs-build-plan` | 全仓 | `<superpowersRoot>/plans/build-plan.md`（+ `plans/*.md` 派生）、`<openSpecRoot>/changes/<change>/tasks.md` | CodeGraph | `ufs-writing-plans` |
| **`ufs-coding`** | 限当前 Build Task 范围 | **源码 + 测试代码** | Test / Build | `ufs-tdd` |
| `ufs-code-review` | 全仓 | `comet-artifacts/evidence/<change>/review/**`（审计副本；**不写** `comet.review.v1`——它是 evidence 记录，不是文件） | — | `ufs-code-review` |
| `ufs-verification` | 全仓 | `comet-artifacts/evidence/<change>/verify/**`（审计副本；**不写** `comet.verify.v1`——它是 evidence 记录，不是文件） | Test（Git 只读） | `ufs-verification` |
| `ufs-document-review` | 全仓 | findings | — | — |
| `ufs-firmware-expert` | 全仓 | 分析结论 | 知识查询 | — |
| `ufs-failure-analysis` | 失败上下文 | 分类结论 | — | — |

**三重已生效保护 + 一项 Phase 2 提案**（成员与分册 03 §8.2 一致；不得把提案计入"防线"）：

| # | 保护 | 状态 | 载体 |
|---|---|---|---|
| P1 | **写入路径白名单**：每个 Agent 只有一个合法写路径 | ✅ 已生效 | 上表的"写（唯一允许路径）"列；分册 03 §8.1 |
| P2 | **任务范围绑定**：`ufs-coding` 的读范围限当前 Build Task，写范围限 `scope.files` | ✅ 已生效 | `scope.files`（分册 02 §9.6.3）；`H04` 第 ④ 级 |
| P3 | **Hook 放行**：任何写入前必须经 Code Write Hook（`H04`），超范围一律 `BLOCK` | ✅ 已生效 | `H04`/`H05`（分册 03 §7.3.4）；`derived_allow ≡ ∅` 见 `99 D72`（**`D72` 裁的是 H06，不是 H04**） |
| P4 | **执行上下文绑定**（原第 4 项） | ⚠️ **不是独立防线** | 由 P1+P2+P3 共同实现；**不得**单列为第四道保护（分册 03 §8.2） |
| — | **Capability Token** | ⬜ **Phase 2 提案，未生效** | 架构稿建议的一次执行能力凭证（阶段 + 任务 + 写路径 + 有效期）。本设计**保留为提案**，不冻结为契约（`99 D68`）；冻结前以 P1–P3 为准 |

**计数口径**：**三重已生效保护**，加一项 Phase 2 提案。任何位置写"四重保护"均指把提案误计为防线，**已撤回该表述**。

### 6.6 OMO Team Mode

OMO **不作为生命周期管理器**，定位为：
> **局部节点内的 Agent Collaboration Runtime。**

```text
Comet 负责：什么时候启动 Team / 什么时候结束 Team / Team 输出是什么
OMO 负责：Agent 如何协作 / 如何通信 / 如何并行
```

职责切分示例：
```text
design 节点：Design Agent ├── Architecture Agent
 ├── Firmware Expert
 └── Test Design Agent
```

目标：**避免重复建设 Agent Mailbox / Collaboration Protocol**。

---

## 7. Skill 架构

### 7.1 Skill 集（2 + 6）

源文档称"冻结五个核心 Skill"。本设计**有意取代**该表述，扩展为 8 个：其中 **4 个**（② `ufs-challenge`、⑤ `ufs-writing-plans`、⑦ `ufs-code-review`、⑧ `ufs-verification`）含新增或推补内容，由 Comet 节点对齐与审查者补全推导而来。

| # | Skill | 服务节点 | 承担 Agent | 状态 |
|---|---|---|---|---|
| ① | `prd-split` | Stage 0-a | `ufs-requirements` | **已存在**（`.opencode/skills/prd-split/SKILL.md`） |
| ② | `ufs-challenge` | Stage 0-b | `ufs-challenge` | **新增**：把 `grill-me-atdd-challenge-template.md` 规范化为 Skill |
| ③ | `atdd-development` | `open` | `ufs-main` | 源已有 |
| ④ | `ufs-test-design` | `design` | `ufs-test-design` | 源已有 |
| ⑤ | `ufs-writing-plans` | `plan` | `ufs-build-plan` | `exists`（源已有；Outputs 章节由本设计补齐后即生效） |
| ⑥ | `ufs-tdd` | `execute` | `ufs-coding` | 源已有 |
| ⑦ | `ufs-code-review` | `review` | `ufs-code-review` | **新增**：UFS 特化 `requesting-code-review` |
| ⑧ | `ufs-verification` | `verify` | `ufs-verification` | **`active`**：源规范缺失，第 12 章由本设计补写并**立即冻结为权威规范**（`99 D35`），**不以“待原作者评审”为生效前置** |

四个 Skill（② 新增、⑤ 补 Outputs、⑦ 新增、⑧ 补全）在分册 02 中逐节给出推导依据。**状态词表统一为 `exists` / `new` / `active`**（分册 02 §0.4），不使用 `backfilled`。

### 7.2 Skill 的边界

**Skill ≠ 生命周期 / 知识库 / 通信 / 安全**：
| Skill 不承担 | 承担者 |
|---|---|
| Knowledge Base | OpenViking |
| Lifecycle Management | Comet |
| Agent Communication | OMO |
| Security Enforcement | Hooks |

补充规则：
- Skill 决定"什么时候调用 / 为什么调用 / 如何使用结果"；Plugin 决定"如何真正执行"
- Skill 描述方法，**不保存企业知识**
- **Skill 输出必须结构化**——所有节点都必须产生结构化 Artifact，以便被 Hook 校验与被下游节点引用（对应不变量 I6）

调用链固定为：`Comet Node → Skill → Agent → Tool / Plugin`


### 7.3 节点 × Agent × Skill 矩阵

| 节点 | 主 Agent | Skill | 门禁 Agent | 专家（按需） |
|---|---|---|---|---|
| Stage 0-a | `ufs-requirements` | `prd-split` | — | `ufs-firmware-expert` |
| Stage 0-b | `ufs-challenge` | `ufs-challenge` | — | `ufs-firmware-expert` |
| `open` | `ufs-main` | `atdd-development` | `ufs-document-review` | `ufs-firmware-expert` |
| `design` | `ufs-exploration` → `ufs-design` → `ufs-test-design` | `ufs-test-design` | `ufs-document-review` | `ufs-firmware-expert`、OMO Team |
| `plan` | `ufs-build-plan` | `ufs-writing-plans` | `ufs-document-review` | — |
| `execute` | `ufs-coding` | `ufs-tdd` | Code Write Hook | `ufs-firmware-expert`、`ufs-failure-analysis` |
| `subagent-execute` | OMO / 子代理 | `subagent-driven-development` | Handoff Evidence Hook | — |
| `review` | `ufs-code-review` | `ufs-code-review` | Review Evidence Hook | — |
| `verify` | `ufs-verification` | `ufs-verification` | Verify Hook | `ufs-failure-analysis` |
| `archive` | `ufs-main` + Human | `comet-archive` | Close Hook | — |

`ufs-firmware-expert` 是**横切专家**，可在任意节点**按需**挂载（Stage 0-a/0-b、`open`、`design`、`execute`），不占节点槽位；这是本表 Stage 0-b 一栏列它的依据，与 §4.0 的"按需·全程"一致。 `ufs-failure-analysis` 只在失败时挂载于 `execute` / `verify`。

---

## 8. 产物架构与唯一权威来源

### 8.1 三套目录的关系与裁决

源文档的目录主张有**四行**（其中后两行是本设计的补充），存在不一致，这是本设计需要收口的第一类差异：
| 目录 | 来源 | 内容 |
|---|---|---|
| `<openSpecRoot>` | Comet 本体 | proposal / delta spec / tasks.md / `.comet.yaml` |
| `<superpowersRoot>`（恒为 `docs/superpowers/`） | Comet 本体 | Design Doc（`specs/`）、实施计划（`plans/`）、报告（`reports/`） |
| `comet-artifacts/requirements/` | `prd-split` agent | 需求基线 |
| `comet-artifacts/{open,design,build,verify}/` | 草稿 V1.0 §46 | 与上两套重叠 |

**裁决**：
1. **不再新建 `comet-artifacts/{open,design,build,verify}/`**。这些内容已有归属，见 §8.2 的 `pathBase` 权威表。
2. **`comet-artifacts/requirements/` 保留**——它由 `prd-split` 的 agent 权限白名单硬绑定（`edit: comet-artifacts/requirements/**`），且位于 change 之外，是 Stage 0 的合法产物目录。
3. 新增 `comet-artifacts/challenge/` 承载 Stage 0-b 的 `challenge-report.md`。
4. **唯一权威来源原则**（V1.0 §44 原话）：
> 不要让 OpenSpec、Comet Artifact、Design Doc 三处同时成为技术设计的权威来源。

### 8.2 产物物理位置（Comet `pathBase` 权威表）

Comet 的 Output Schema 用 `pathBase` 冻结了每个产物的物理根。**这是唯一权威**，不得按"哪个根更顺手"自行安排——写错根会直接导致写路径白名单、Hook 路径与唯一权威来源原则全线错位。

| Schema | 产物 id | paths | **pathBase** | 解析后位置（docs 布局） |
|---|---|---|---|---|
| `comet.intake.v1` | `comet-state` | `changes/*/.comet.yaml` | `classic-openspec-root` | `docs/openspec/changes/<change>/.comet.yaml` |
| `comet.design.v1` | `design-doc` | `specs/*.md` | **`classic-superpowers-root`** | **`docs/superpowers/specs/`** |
| `comet.design.v1` | `delta-spec` | `changes/*/specs/*/spec.md` | `classic-openspec-root` | `docs/openspec/changes/<change>/specs/` |
| `comet.plan.v1` | `implementation-plan` | `plans/*.md` | **`classic-superpowers-root`** | **`docs/superpowers/plans/`**，其中 **`plans/build-plan.md` 是本设计的实施契约权威**（`99 D04`），其余 `plans/*.md` 是派生视图 |
| `comet.plan.v1` | `openspec-tasks` | `changes/*/tasks.md` | `classic-openspec-root` | `docs/openspec/changes/<change>/tasks.md` |
| `comet.execution-evidence.v1` | `task-state` | `changes/*/tasks.md` | `classic-openspec-root` | 同上 |
| `comet.handoff.v1` | — | — | — | **无产物文件，仅 evidence** |
| `comet.review.v1` | — | — | — | **无产物文件，仅 evidence** |
| `comet.verify.v1` | — | — | — | **无产物文件，仅 evidence** |
| `comet.archive.v1` | — | — | — | **无产物文件，仅 evidence** |


**三点必须记住**：
1. **`superpowersRoot` 恒为 `docs/superpowers/`**，**不随 `classic.artifact_layout` 变化**；只有 `openSpecRoot` 会随布局在 `docs/openspec/`（docs）与 `openspec/`（legacy）之间切换。`superpowersRoot` 自带 `specs/`、`plans/`、`reports/` 三个子目录。
2. **`review` / `verify` / `archive` 三个 schema 的 `artifacts` 是空数组**——它们**不是文件**，而是由 Runtime 从真实文件**运行时推导**的 evidence 记录（`collectClassicEvidence()` 检查 `openspec.proposal` / `openspec.design` / `openspec.tasks` / delta spec / `build.plan` 是否存在且合规）。**实现者不要去找 `review.json` / `verification.json` / `.comet/evidence/<change>/*.json` 这类文件——它们不存在**。
3. **Canonical Design Doc 在 `<superpowersRoot>/specs/`，不在 `<openSpecRoot>`**。OpenSpec 的 `design.md` 是另一件事，见 §8.4。

### 8.3 产物所有者表（含本设计新增）

Comet 的 Output Schema 只覆盖 §8.2 那张表。**本设计新增的产物在 Comet 原生 schema 中没有槽位**——这一点由 `99 D01` 裁定：**扩展载体唯一**为 `.opencode/comet-ufs/schema-ext.json`（**通过插件注册，不修改 Comet Runtime**；Phase 1 明写不改 Runtime）；**校验能力分三级**：**L1** 文件存在（`artifact-exists` 等价）、**L2** 结构合法（本设计的文件级 schema）、**L3** 复用 Comet 的 `artifact-structured`（**依赖 `EXT-04` 的 schema 注册能力**）。**L1/L2 是 MVP 必须**，因此“新增产物无法强制”不成立；L3 未启用前不得声称“可机器校验”。下表最后一列给出扩展点。

> 因此本节的动词是**注册到扩展文件**，不是“必须扩展 Comet 的 Output Schema”。Comet schema 本身保持不动（`99 D01`/`D10`）。

| 产物 | 位置 | 权威所有者 | 写入者 | schema 来源 |
|---|---|---|---|---|
| Story / AC / Scenario | `comet-artifacts/requirements/` | **Stage 0-a** | `ufs-requirements` | 注册到 `schema-ext.json`（Stage 0 schema；L2） |
| Challenge 报告 | `comet-artifacts/challenge/` | **Stage 0-b** | `ufs-challenge` | 注册到 `schema-ext.json`（Stage 0 schema；L2） |
| `proposal.md` | `<openSpecRoot>/changes/<change>/` | 镜像（权威在需求基线） | `ufs-main` | Comet 原生 |
| delta `specs/**/spec.md` | `<openSpecRoot>/changes/<change>/specs/` | 镜像（权威在需求基线） | `ufs-main` | Comet 原生（`delta-spec`） |
| `.comet.yaml` | `<openSpecRoot>/changes/<change>/` | Comet Runtime | `ufs-main`（经 CLI） | Comet 原生（`comet-state`） |
| **Canonical Design Doc** | **`<superpowersRoot>/specs/*.md`** | **`design` 节点** | `ufs-design` | Comet 原生（`design-doc`） |
| `test-design.md` | `<superpowersRoot>/specs/` | **`design` 节点** | `ufs-test-design` | 注册到 `schema-ext.json`（作为 `comet.design.v1` 的扩展条目；L2，L3 待 `EXT-04`） |
| `exploration-report.md` | `<superpowersRoot>/reports/` | **`design` 节点 Step 0** | `ufs-exploration` | 注册到 `schema-ext.json`（作为 `comet.design.v1` 的扩展条目；L2，L3 待 `EXT-04`） |
| **`build-plan.md`** | **`<superpowersRoot>/plans/build-plan.md`** | **`plan` 节点**（实施契约唯一权威） | `ufs-build-plan` | Comet 原生 `implementation-plan`（`pathBase: classic-superpowers-root`）；UFS 侧**注册到 `schema-ext.json`** 以承载 17 节结构（`99 D04`） |
| `plans/*.md`（派生视图） | `<superpowersRoot>/plans/` | `plan` 节点 | `ufs-build-plan` | Comet 原生（`implementation-plan`）；**不得**承载 `build-plan.md` 未出现的 Task / 字段 / Regression 等级（`99 D04`） |
| `tasks.md` | `<openSpecRoot>/changes/<change>/tasks.md` | **`plan` 节点**（任务状态权威） | `ufs-build-plan` | Comet 原生（`openspec-tasks`） |
| 源码与测试代码 | 仓库 | **`execute` 节点** | `ufs-coding` | — |
| Code Review 结论 | **无产物文件**；evidence 记录 | **`review` 节点** | `ufs-code-review` | Comet 原生（`comet.review.v1`，evidence-only） |
| Verification 结论 | **无产物文件**；evidence 记录 | **`verify` 节点** | `ufs-verification` | Comet 原生（`comet.verify.v1`，evidence-only） |
| 归档产物 | `<archiveRoot>` | **`archive` 节点** | `ufs-main` | Comet 原生（`comet.archive.v1`） |

**位置裁定理由**：新增产物一律与**同阶段的原生产物同根**——`design` 阶段产物放 `<superpowersRoot>/specs/` 与 `<superpowersRoot>/reports/`，与 `design-doc` 同根；不新建第四个根，也不放进 change 目录。这样"唯一权威来源"原则与写路径白名单都能保持单一。

### 8.4 OpenSpec `design.md` 与 Canonical Design Doc 的区别

两者**不可同时充当技术设计权威**：
| 对象 | 位置 | 角色 |
|---|---|---|
| OpenSpec `design.md` | `<openSpecRoot>/changes/<change>/design.md` | 工程上下文 / 影响 / 约束摘要，或 **Canonical Design 的索引** |
| **Canonical Design Doc** | `<superpowersRoot>/specs/*.md`（由 `design_doc` 状态字段指向） | **技术设计的唯一权威** |


### 8.5 `tasks.md` 的归属裁决

> `tasks.md` 应成为 **Build 的实现权威**，而不是 Open 的需求拆解产物。

理由：Classic 可能依赖 `tasks.md` 完成状态闭包；若 Open 阶段就临时产生未细化任务，会导致 `tasks.md` 权威性不清。本设计对齐 Comet 节点契约：`tasks.md` 由 `plan` 节点（`ufs-build-plan`）产出并维护，`open` 阶段不产出任务。


---

## 9. 状态机与回流

### 9.1 正常链

```text
open ──open-complete──► design ──design-complete──► build ──build-complete──► verify
  │                                                                                │
  │  （tweak / hotfix：open-complete 直接进 build，跳过 design）                     │
  │                                                                        verify-pass
  │                                                                                │
  │                                                                                ▼
  │                                                                            archive
  │                                                                                │
  │                                                                             archived
  │                                                                                │
  └────────────────────────────────────────────────────────────────────────────► 终态
```

**迁移表见 §2.2**：**9 个事件**（`open-complete` / `design-complete` / `build-complete` / `verify-pass` / `verify-fail` / `preset-escalate` / `archive-confirm` / `archive-reopen` / `archived`），列为 `事件 / from / guard / to`，另有四个事件的副作用表。`archive` 阶段的另一条合法迁移是 `archive-reopen`（→ **`verify`**，见 §2.2 与 §9.2）。

`hotfix` / `tweak` 下 `open-complete` 直接进入 `build`，跳过 `design`。

### 9.2 回流路径

| 事件 | 路径 | 语义 | 承担 Agent |
|---|---|---|---|
| `verify-fail` | verify → **build** | 验证未通过，回到实现（Comet 原生） | `ufs-verification` 触发 |
| `preset-escalate` | build → **full** | 预设升级：修复中发现变更超出 hotfix/tweak 适用条件 | `ufs-main` + Human 决策 |
| 需求冲突回流 | open / design → **Stage 0** | 需求冲突或 AC 无法覆盖 | `ufs-challenge` 重新质询 |
| `DESIGN_BLOCKER` | design → 停止 | 需求错误或不完整，不得静默重新解释 | `ufs-design` |

**硬规则**：`build` / `verify` 发现实现问题时，**不以改写需求作为默认修复方式**。

### 9.3 失败四分类与路由

| 分类 | 含义 | 路由 |
|---|---|---|
| `TEST_PROBLEM` | 测试本身写错 / 断言不对 / 测试不可靠 | → Test Design（④ `ufs-test-design`）（**不得改源码**） |
| `CODE_PROBLEM` | 实现与设计不符 | → `ufs-coding` |
| `DESIGN_PROBLEM` | 设计无法满足 AC | → **停止 Build，请求 Design Review** |
| `ENVIRONMENT_PROBLEM` | 工具链 / 环境 / 硬件不可用 | → Infrastructure / Environment |

**关键约束**：分类发生在**纠正动作之前**；`ufs-failure-analysis` 不得直接修改源代码。

### 9.4 Test Contract Protection

| 禁止模式 | 唯一允许的测试变更路径 |
|---|---|
| `Test Failed → 修改 Expected → PASS` | 测试契约的变化**必须有 Requirement / Design 变化依据**，并留下变更记录 |


---

## 10. Human Gate 与决策点

### 10.1 决策点协议（引用，见 §2.6）

**唯一权威 = §2.6「用户决策点协议（阻塞式）」**；本节不重复条款，只给节点侧落点：
`open`（change 形态）/ `design`（验证策略）/ `plan`（Build Plan）/ `execute`（Test Contract 或 Scope 变更）/ `verify`（验收）/ `archive`（归档）六处 Human Gate 触发时，一律按 §2.6 执行（含"推荐规则 vs 推荐值"的口径：**可用推荐规则生成选项，不得用推荐值代替确认**）。

### 10.2 各节点的 Human Gate

| 节点 | Gate | 闸门对象 |
|---|---|---|
| Stage 0-a | Human Review | 需求基线 |
| Stage 0-b | **Story Freeze** | Story 边界 / Scope / AC / 场景 / 关键架构边界 |
| `open` | 需求确认 | intake 结果与 change 形态 |
| `design` | **架构批准** + **验证策略批准** | Design Doc、Verification Strategy / `test-design.md` |
| `plan` | **Build Plan 批准** | `plans/*.md`、任务清单 |
| `execute` | 阶段内决策点 | Scope 变更、Test Contract 变更、预设升级 |
| `review` | — | （自动，由 Hook 保证证据） |
| `verify` | **验收** | `READY_FOR_HUMAN_ACCEPTANCE` |
| `archive` | **最终决策** | 归档条件 7 项 |

### 10.3 Story Freeze 的阻断条件

下列问题**至少**阻断 Freeze：
- AC 没有场景，或通过条件不可观察
- Scope / Out of Scope 不清晰
- 依赖未定义或不可获得
- 关键性能、资源、安全、兼容性或架构问题未决
- Story 包含多个不可独立交付的价值切片

冻结后，Scope / Out of Scope / AC / 关键架构边界 / 关键约束 / Human Decision **不得被后续 Agent 静默改变**。

---

## 11. CodeGraph / graphify / OpenViking 的定位

### 11.1 CodeGraph —— 代码事实引擎

回答：函数在哪 / 谁调用它 / 它调用谁 / 结构体在哪定义 / 变量在哪修改 / 接口影响哪些模块 / 改动可能影响什么。

**不是** Requirement Source；只提供 Implementation Evidence / Change Impact / Existing Behavior / Dependency Information。

| 节点 | CodeGraph 用途 |
|---|---|
| `design`（Exploration） | 代码/调用/依赖探索，建立既有行为事实 |
| `design` | 必读输入，支撑影响与既有架构判断 |
| `plan` | 生成 Change Impact / File List / Function List / Dependency Graph |
| `execute`（Legacy） | Legacy 起始点：CodeGraph → Existing Behavior → Characterization Test |
| Hook 层 | 作为 Code Write Hook 的输入，做 Scope 漂移检测 |
| `verify` | 保留 查询 → 结果 → 仓库版本 → 证据 |

**重要限制**：CodeGraph 查询结果属于**动态事实**，必须带完整溯源字段：
```yaml
source: codegraph
query:
result:
repository:
commit:
timestamp:
```

`verify` 不能只写"AI 认为这个函数影响 Scheduler"，而应保留 `CodeGraph Query → Result → Repository Revision → Evidence`。

### 11.2 graphify

定位为**代码结构快速探索能力**：Module Overview / Call Graph / Dependency Exploration / Architecture Navigation。

```text
graphify → Fast Exploration
CodeGraph → Structured Code Facts
```

**调用优先级（已裁定，非未决）**：

| 场景 | 用哪个 |
|---|---|
| `design` 节点 Step 0 的**快速结构探索** | `graphify` |
| 需要**可追溯事实、影响面分析、写入 Evidence** | **必须** `CodeGraph` |
| 一次查询中同时调用两者 | **禁止**（避免双份事实来源） |

裁定依据：`99 D64`（分册 03 §14.1 `IR-11`）。两者的**职责边界与优先级已冻结**，不属于外部依赖事项。

### 11.3 OpenViking 与知识与代码分离

- OpenViking 是**知识与历史**，不是代码事实
- **知识查询必须带来源（provenance / citation）**
- `ufs-firmware-expert` 的知识优先级：① 已批准内部文档 → ② OpenViking → ③ OpenWiki → ④ CodeGraph → ⑤ 其他已批准来源
- 输出必须区分 `FACT / REFERENCE / ANALYSIS / ASSUMPTION`；**绝不把假设当作事实呈现**

> **张力记录**：把 CodeGraph 列为知识来源第 4 位，与"知识与代码必须分离"存在张力。本设计以**分离原则为权威**（原则性条款优先），CodeGraph 在此仅作"经批准的事实来源"引用。

---

## 12. 第一版实现范围（MVP）

**Hook（第一版只做 6 个）**：
```text
1. stage-gate
2. build-gate
3. code-write-scope
4. test-contract-protection
5. pre-verify
6. pre-close
```

原文结论：这 6 个已可形成基本安全闭环；**不要一开始实现十几个**；尤其要详细设计这条链：
```text
code-write → Build Task → Scope → CodeGraph → Hook → Allow / Block
```

**Plugin（第一版 5 个）**：`Comet`、`CodeGraph`、`OpenViking`、`Test Runner`、`Evidence`；**OMO 作为第二阶段接入**。

**第一版实际运行链**：
```text
User → Comet → Main Agent → ATDD Skill → `ufs-document-review` 门禁 → Human
→ Design Agent → Test Design → Review → Human
→ Build Plan → Human → Coding Agent → code-write Hook → CodeGraph → ALLOW
→ TDD → Test Runner → Evidence → Verify Agent → pre-verify Hook
→ Human Acceptance → pre-close Hook → ARCHIVE
```

原文结论：这已经是一个可以在真实 UFS Firmware 项目上做 Pilot 的最小闭环。

---

## 13. 演进路线

### Phase 1：兼容式落地

```text
Stage 0（prd-split + Challenge）
 ↓
External Validator
 ↓
Comet open → design → build → verify → archive
```

**不修改 Comet Runtime**。Stage 0 作为受强校验的前置阶段存在，产物通过 validator 后交给 `comet-open`。

### Phase 2：Requirement Manifest

Comet Runtime 增加：`Requirement Manifest` / `Story Manifest` / `Freeze Record` / `Requirement Validator`。

### Phase 3：正式状态机

把 Stage 0 的状态纳入 Comet Runtime（对应草稿的 `DECOMPOSITION` / `HUMAN_REVIEW` / `STORY_FROZEN`）。

### Phase 4：全链路 Guard

强制并可机器检查：
```text
REQ → STORY → AC → DES → TEST → EVIDENCE
```

### Phase 5：平台化

```text
Human → Main Agent → Comet Runtime
 ├── open / design / build / verify / archive
 └── 能力层：CodeGraph / graphify / OpenViking / OMO
 / Plugins / Hooks
```

**本设计补充的实施层承诺**：下一步应进入 **Comet × OpenCode × Skills × Plugins × Hooks 的实际目录与运行时设计**：
```text
.opencode/{agents,skills,plugins,hooks,config}
.comet/{state,enforcement}          # 运行态，默认忽略（99 D76 / IR-4）
.comet/{approvals}                  # 审计依据，必须入库
comet-artifacts/                   # 产物与审计副本根，必须入库
  requirements/  challenge/
  evidence/<change>/{index.md,challenge,document-review,review,verify,test-results,regression,codegraph,approvals}/
  traceability/traceability.yaml   # 追溯唯一序列化载体（99 D09）
project/{firmware,tests,...}
```

**修正说明（A13）**：原骨架把 `evidence` / `traceability` 放在被 `.gitignore` 的 `.comet/` 下，且缺 `enforcement`——与 `99 D02`/`D09`/`D76` 相反。**权威目录树见分册 03 §10.3**；本节只给顶层形状，不重复展开。

运行链：
```text
User Request → Comet State → Main Agent → Skill → Sub Agent
 → Plugin → Hook → Artifact → Next Node
```

该层完成后才能针对当前 Comet 版本（0.4.0-beta.19 / 最新）做适配，**而不是再重新讨论方法论**。

---

## 14. 设计原则（合并）

合并 V1.0 第 51 节的 15 条与架构稿 Principle 1–10，去重后为下列 17 条。**本节不逐条标注来源**——按全文档集的引用口径（§0.4、`99` 表首），正文只给结论，参考资料在附录 B 以文档级列出；每条与哪份资料的哪一条对应，属于检索路径而非设计内容。

1. **ATDD 是整个流程的主线，不是测试阶段的附属功能。**
2. **Story 是需求与工程之间的最小业务闭环。**
3. **AC 是冻结后的行为契约。**
4. **AI 可以执行，但不能未经授权定义需求。**
5. **AI 可以修改代码，但必须依据 Approved Design 和 Build Plan。**
6. **Design 必须做到 AC → Design 全覆盖。**
7. **Verify 必须从 AC 出发，而不是从代码出发；且必须独立于 Build 的局部成功。**
8. **每一次代码和测试都必须能够追溯到 Story / AC。**
9. **测试失败首先进行分类，而不是立即修改代码。**
10. **Test GREEN 不是最终正确性的充分条件。**
11. **问题必须按类别回流，不能在错误阶段偷偷修正。**
12. **CodeGraph 是代码事实，不是需求事实。**
13. **OpenViking 是知识和历史，不是代码事实。**
14. **Skill 描述方法，不保存企业知识。**
15. **Hook 用于强制执行，而不是只给 Agent 提示。**
16. **OpenSpec / Comet Artifact / Design Doc 必须有明确的唯一权威来源。**
17. **Agent 可以提出方案，Human 决定关键目标与架构；Human 保留架构、行为变化、风险和最终验收决策权。**
18. **Hook / Plugin 负责 Enforcement，不负责替人决策。**
19. **对 SSD/UFS Firmware，纵向价值切片优先于按源码文件拆分。**
20. **最终目标不是 Agent 自动完成流程，而是让 Agent 在可验证的工程边界内持续推进。**

---

## 附录 A：关键设计裁定

参考资料（4 份 `.txt` 讨论稿、`V1.0`、Comet 本体）之间存在大量不一致与留白。本平台的做法是**先裁定共识、再输出设计**，而不是把分歧留给读者。

本附录列出**决定架构形态的关键裁定**。完整登记（86 条，含分册条目反向索引与 C1–C20 覆盖核验）见配套的 [`99-共识裁定表.md`](99-共识裁定表.md)。

**裁定原则**（按优先级）：

1. **有可验证的实现依据者优先**——如 Comet 源码常量、Output Schema、guard 定义。
2. **与已冻结的上层契约一致者优先**——不得为局部便利破坏架构不变量 I1–I8。
3. **工程上更可机器检查者优先**——本平台的价值在于强制层能否真正落地。
4. **参考资料中较晚且更具体者优先**——基线文档（标注 Design Baseline）在后，架构稿次之，讨论稿最早。
5. **均无依据时，选代价最小且最容易回退的方案**，并在此记录为设计选择。

| # | 分歧 | 参考资料的说法 | **裁定** | 理由 |
|---|---|---|---|---|
| 1 | 阶段数量 | 草稿推出"六阶段"（把 `plan` 当独立阶段、把 `archive` 叫 "Close"）；Comet 常量为五阶段 | **phase = 5**（`open/design/build/verify/archive`）；**node = 8**（`open/design/plan/execute/subagent-execute/review/verify/archive`） | `PHASES` 是 Comet 代码常量，Workflow Node 是原生契约。草稿混淆了 phase 与 node 两个维度 |
| 2 | "Open" 的语义 | 草稿的 Open = 工程调查；Comet 的 `open` = intake + 选 change 形态 + 初始化 state | **Comet `open` 保持原义**；工程调查归入 **`design` 节点 Step 0**（`ufs-exploration`） | `open` 的 guard 是 `.comet.yaml exists`，不承担影响分析；而 `design` 的输入本就含既有架构与 CodeGraph |
| 3 | 需求拆分与质询的位置 | V1.0 视其为 Comet 前置；架构稿未定义 | **Stage 0 位于 Comet phase 之外**，早于任何 change 的存在；离开闸门为 Human Story Freeze | Stage 0 产物目录与 change 目录**并列**，且必须在 change 创建前产出——`open` 的输入正是它 |
| 4 | Agent 总数与名单 | V1.0 列 8 个；架构稿列 10 个，自称 "1 主控 + 7 阶段 + 2 专家" | **13 个** = 1 编排 + **5 执行** + 3 审查 + 2 专家 + 2 前置。V1.0 旧名册作废 | 架构稿把横切的文档审查计入"阶段执行者"。按职能层重分类后，专家不占节点槽位 |
| 5 | 代码审查的归属 | 参考资料中无归属：Verify 要求 Code Review，但 `ufs-document-review` 明文"不审代码" | 由 `ufs-code-review` 承担，实现 Comet 原生的 **`review` 节点** | Comet 原生已有该节点与 `comet.review.v1` schema。这是一个原本**无法执行**的闸门 |
| 6 | Challenge 是否独立成 Agent | 参考资料未讨论 | **独立**为 `ufs-challenge` | 与文档审查形式相似但**问题不同**：Challenge 质询"需求本身对不对"，文档审查检查"制品之间自洽吗" |
| 7 | Skill 数量与骨架 | 参考资料称"冻结五个核心 Skill"；四份规范骨架为 28/50/37/52 节且命名不一 | **8 个 Skill**，**统一 18 节模板** | 原"五个"漏掉需求前端与代码审查；骨架不一致会让实现者按不同维度理解 Skill |
| 8 | `ufs-verification` | 参考资料只有大纲，标注"← 下一步"，**无规范** | 按已冻结的 `verify`/`archive` 契约 + 兄弟 Skill 的 DoD **补全** | 五 Skill 闭环缺最后一环；Verify 是证据契约的落点，不能留空 |
| 9 | UFS TDD 层数 | 讨论稿 **5 层**（含 ATDD）；基线文档只列 **4 项** | **5 层**：Unit / Component / Integration / Hardware-Firmware Verification / ATDD | 基线文档的 4 项是"Build 阶段可实现方法论"的子集；5 层才是完整模型 |
| 10 | Regression 是否有默认策略 | 参考资料只说"等级由 Test Design / Build Plan 确定"，**未给默认** | **不设默认。** 等级一律由 **Build Task 显式声明**；Verification Level × Regression 映射只表达"**证据下限**" | 自动推导会使回归闸门失去人为判断；参考资料既未给默认，"没有默认"就是唯一安全的默认 |
| 11 | 两套同号 "Rule 1–7" | 基线文档的五条正式规则与 Skill 稿的七条硬规则**同号不同义** | 编号前缀消歧：**`FR-1`…`FR-5`**（正式规则）、**`SR-*`**（Skill 硬规则）、**`SYS-*`**（体系级规则）。**禁止裸用 "Rule N"** | 同号不同义会造成实现歧义 |
| 12 | 覆盖率阈值 | 参考资料称"Coverage 可作指标，不能作唯一完成条件"，**未给阈值** | **不设覆盖率阈值。** DoD 以"**每个 P0/P1 行为都有对应层级的验证证据**"为准 | 阈值会诱导为达标而写无断言测试；行为↔证据对齐本身即可机器检查 |
| 13 | 产物的物理位置 | 草稿把 `design.md`、`plans/*.md` 放在 OpenSpec 根 | 以 Comet 的 **`pathBase`** 为准：`design-doc` 与 `implementation-plan` 在 **`<superpowersRoot>`**（`docs/superpowers/`）；`tasks.md`/`.comet.yaml`/delta spec 在 `<openSpecRoot>/changes/<change>/` | schema 直接给出 `pathBase`；写错根会使写路径白名单与 Hook 路径全线错位 |
| 14 | `review`/`verify`/`archive` 是否有产物文件 | 草稿按目录为其安排文件 | **没有产物文件。** 其 schema 的 `artifacts` 是空数组，判定依据是运行时推导的 evidence；**另设审计用落盘证据库并纳入版本控制** | 前者是 Comet 事实（避免实现者去找不存在的文件）；后者是满足 G3 离线审计的新增设计 |
| 15 | Node 身份的持久化 | Comet 只持久化 `phase`，而 `build` 覆盖 `plan`/`execute`/`subagent-execute`/`review` 四个节点 | **引入 Node Projection**：由 **Comet Plugin 判定**节点身份（只有它知道当前执行哪个 Skill），**经 Runtime 状态写路径与 `phase` 同事务落盘**；**Hook 只读**，缺失或不一致时 fail-closed | Plugin 解决"Runtime 不知道节点"，Runtime 解决"两处状态分叉"；由 Agent 自报会使放行依据退化为被审查方的声明 |
| 16 | `build_mode` 枚举不一致 | `classic-state.ts` 的 `BUILD_MODES` 不含 `autonomous`，而 `comet-build/SKILL.md` 全篇指示写入该值，导致**无法回读** | **以 Runtime 常量为准，禁止写入 `build_mode autonomous`**；节点区分改用 `tasks.md` + `build_pause` + Node Projection | 扩展枚举需要**修改 Comet 源码**，违反 Phase 1"兼容式落地"前提；且 `autonomous` 的语义（不加载外部计划 Skill）与本设计的 `plan` 节点直接冲突 |
| 17 | 引用风格 | 草稿大量使用 `来源: 文件 L###-L###` 逐节标注 | **正文不逐节标注来源。** 参考资料在文档级列出（附录 B），分歧与裁定集中登记 | 逐节行号标注使文档读起来像审计报告，且设计文档一经修订行号即失效 |

## 附录 B：参考资料

### B.1 方法论参考资料

| 文档 | 性质 |
|---|---|
| `docs/Comet + ATDD + UT Design.txt` | v1.0，标注 Design Baseline；方法论主干 |
| `docs/Comet + ATDD + UT Design_讨论.txt` | 讨论稿；TDD / Legacy / UT 设计的推导过程与备选方案 |
| `docs/Comet + ATDD + UT Design_总体架构设计.txt` | 平台架构、Agent 契约、Plugin / Hook 实现设计 |
| `docs/Comet + ATDD + UT Design_skills.txt` | 5 个企业 Skill 的规范正文（含三对重复副本） |
| `docs/AI辅助UFS_SSD固件开发_ATDD_Comet_完整设计文档_V1.0.md` | V1.0 总体设计；本设计的直接前身，降级为历史版本 |
| `docs/prd-split.md`、`docs/prd-split-implementation-guide.md` | 需求拆分 Agent 的设计与落地方案 |
| `docs/grill-me-atdd-challenge-template.md` | Challenge 模板 |
| `.opencode/skills/prd-split/SKILL.md`、`.opencode/agents/prd-split.md` | 已实现的 `prd-split` Skill 与 Agent 定义 |

### B.2 Comet 本体的权威来源

| 事实 | 来源文件 |
|---|---|
| 五阶段常量、`ClassicState` 字段、`BUILD_MODES` 等枚举 | `domains/comet-classic/classic-state.ts` |
| 迁移事件与 guard | `domains/comet-classic/classic-transitions.ts` |
| 八个 Workflow Node、Output Schema、`pathBase`、guardrail | `domains/workflow-contract/builtins.ts` |
| 产物逻辑根解析 | `domains/comet-classic/classic-layout.ts` |
| evidence 的运行时推导 | `domains/comet-classic/classic-evidence.ts` |
| 产物布局协议、决策点协议、自动衔接 | `.opencode/skills/comet-classic/reference/{classic-layout,decision-point,auto-transition}.md` |
| 各阶段入口 Skill | `.opencode/skills/comet-{open,design,build,verify,archive}/SKILL.md` |

Comet 源码检出于 `/home/zsf/comet-openspec-research`（0.4.0-beta.19）。

## 附录 C：外部依赖事项

以下事项**不取决于本设计**，而取决于本设计之外的事实。它们不是"未决的设计问题"——设计已经给出默认动作，只是该动作需要外部条件到位。

**本附录是外部依赖事项的唯一登记处**：编号为 `EXT-01`…`EXT-13`，全文档集唯一。分册不得另起编号空间（`03 §14.2`、`04 §7.2`、`99 §2` 均为本表的分册视图，编号一律引用本表）。

| # | 事项 | 需要什么事实 | 事实到位后的默认动作 | 分册视图 |
|---|---|---|---|---|
| `EXT-01` | 本仓库 Comet 布局冲突 | `.comet/config.yaml` 声明 `artifact_layout: docs`，但 `docs/openspec/` 已不存在，产物实际落在根目录 `openspec/` | 构成 legacy / docs 双根冲突。先运行只读的 `comet doctor` 诊断，确认后执行 `comet classic root move docs --apply` 迁移，或把配置改回 `legacy`。**在此之前不得写入** | §2.5、`03 §14.2`、`04 §7.2` |
| `EXT-02` | `comet` CLI 是否可用 | 当前环境 `PATH` 中没有 `comet` 命令 | 先安装 Comet；否则 `comet state` / `comet guard` / `comet doctor` 全部不可执行，Stage 0 的 Phase 1 落地与所有 guard 校验都无法运行 | `04 §4.5 H6`、`99 §2` |
| `EXT-03` | 目标 Comet 版本 | 本设计以本机检出的 0.4.0-beta.19 为基线 | 若目标版本不同，按 `classic-state.ts`（阶段、枚举）、`builtins.ts`（Node、schema、pathBase）、`classic-transitions.ts`（迁移表）三处做差异比对后再适配 | `03 §13`、`03 §14.2` |
| `EXT-04` | 新产物的 Output Schema 扩展由谁落地 | 需要修改 Comet 侧或通过插件注册；同时决定 `04` 的 L3 guard 何时启用 | 按 §8.3 的扩展点补 schema 条目；在此之前，新增产物只能由文件级校验兜底，无法进入 `artifact-structured` guard；`04` 的 L1/L2 为 MVP 必须，L3 待该能力可用 | `04 §7.2`、`99 §2` |
| `EXT-05` | UFS 领域事实 | `INSPECTION` 检查项清单、P0/P1 的领域判定细则、测试数据的具体构造 | 由固件团队按已裁定的定义填充具体清单——`INSPECTION` 见 [`99-共识裁定表.md`](99-共识裁定表.md) D20、P0/P1 判定式见 D23、测试数据见 D26。**定义本身已裁定冻结，缺的只是领域内容** | §5.1、`01 §9.2` |
| `EXT-06` | 本地 `comet-build/SKILL.md` 副本与权威副本的漂移 | 本仓库 `.opencode/skills/comet-build/SKILL.md`（208 行）含 `autonomous` 11 处，权威副本 `assets/skills/comet-build/SKILL.md`（293 行）0 处；`BUILD_MODES` 不含 `autonomous` | **本设计侧动作已定**：**禁止写入 `build_mode: autonomous`**（`99 D59`），节点区分改用 `tasks.md` 存在性 + `build_pause=plan-ready` + Node Projection。**外部动作**：执行 `comet update` 重装本地副本即可消除漂移——**不需要扩 `BUILD_MODES`，也不需要改 Comet 上游** | `03 §13.3 G2/G8`、`03 §14.2`、`99 §2` |
| `EXT-07` | Node Projection 的事务写入钩子 | 需 Comet Runtime 在状态迁移点开放写入钩子，供 Plugin 经该路径与 `phase` 同事务落盘 | 钩子可用前，由分册 03 §10.5 运行链中的既有写入点承载 Node Projection；钩子开放后切换为同事务写入（`99 D60`） | `03 §14.2` |
| `EXT-08` | External Validator 的运行形态 | Phase 1 落地形态（本地脚本 / 独立服务）与可调用的 Comet 能力 | schema 与失败动作**已裁定**（`04 §4.3`）；形态确认后按裁定接线，在此之前只以本地脚本形态验证内容契约 | `04 §4.3`、`04 §7.2`、`99 §2` |
| `EXT-09` | Freeze 记录的 Hook 校验挂载点 | Hook 实现编号与触发时机待 `03` 落地；`traceability` / `evidence` 两事件面**不新增 Hook**，语义指派给现有 H07/H11/H12/H13（`99 D70` / `D79`） | 挂载点确定前，冻结由 Human Gate + `freeze-record.md` 承载，不依赖 Hook | `04 §7.2` |
| `EXT-10` | Stage 0 状态纳入 Comet Runtime | §13 Phase 2/3 的 Manifest 与状态机 | Stage 0 当前不写 Node Projection、不参与迁移表；纳入时需与投影机制协调 | `04 §7.2` |
| `EXT-11` | Capability Token 的地位 | 该机制仍为提案 | 不进入 MVP（`99 D68`）；当前以"写路径白名单 + Hook 放行"为准 | `04 §7.2`、`02 §13.1.1` |
| `EXT-12` | "项目架构负责人"对应哪个组织角色 | UFS 组织的评审与签批流程定义（谁有权批准"不可测声明"） | 由 Human 指派；未指派前，`IR-9` 的不可测声明一律 `BLOCK` | `99 §2` |
| `EXT-13` | 目标仓库的 `.gitignore` 是否可修改 | 仓库 `.gitignore` 的归属与改动审批链（可能受组织模板约束） | 默认为可修改；若受约束，则按 `99 D02`/`D76` 的降级口径（审计副本改落本地不入库目录），不得因此取消强制层 | `99 §2` |

---

**文档结束。**

配套分册：[01 方法论](01-方法论-ATDD与测试设计.md) ·
[02 Skill 规范](02-Skill规范.md) ·
[03 Comet 实现设计](03-Comet实现设计-节点特化·Plugin·Hook.md) ·
[04 Stage 0](04-Stage0-需求分解与Challenge.md)
